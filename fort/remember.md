# Longburn operational facts — migrated

The current, provenance-pointed operational ledger is in `fort/memory/facts/`.
Its deterministic derived snapshot is `fort/memory/current.md`. This legacy
pointer is retained only for the three seat launchers, whose prompts still name
this path while longburn-wtx7.6 updates them from outside the mask.

All 29 bullets this file carried on 2026-08-11 were migrated to the ledger with
nothing discarded (longburn-wtx7.2, commit 1a091a9); its own text remains in
git history, and the migration is governed by fortkit `docs/specs/memory.md`
section 8.5.
