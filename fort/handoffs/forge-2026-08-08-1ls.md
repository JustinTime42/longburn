# Handoff: Forge 2026-08-08T16:20:00-08:00

## Plan executed

1. Re-read the governing charter, operational record, Forge protocol, Beads context, founding/review specs, approved worldline specification, and prior Forge handoff.
2. Replace the origin placeholder with a quantized Earth ephemerides HQ resolver and make inbound transport evaluate it inside loop serialization at issue time.
3. Add quantized committed delta-v vectors and a pure Kepler/coast plus constant-acceleration worldline resolver.
4. Correct inbound event provenance so every due burn, applied revision, and refused revision resolves its position at its own event time; add stored-envelope and resolver tests.
5. Run typecheck, lint, tests, and the fort verifier, then commit this record.

## Clarifying questions

None blocking. The authoritative-worldline specification answers the prior session's ownership and replay questions.

Model: gpt-5.6-terra

## State of work

- `longburn-1ls` remains in progress pending the harness-owned verification/review flow. This commit implements the verified HQ and own-time provenance tranche, and introduces the worldline/delta-v primitives.
- `longburn-ksj` is addressed by the own-time callback change: a burn swept at issue time no longer inherits a future command arrival position.

## Verified facts

- `src/sim/headquarters.ts` resolves fixed T0 HQ from Earth ephemerides and quantizes it to integer metres; `src/sim/headquarters.test.ts` proves it is non-origin, time-varying, deterministic, and integral.
- `src/host/plan-revision-transport.ts` requires a live HQ resolver and evaluates it at the serialized issue instant. `src/sim/loop.ts` resolves all due-work provenance at each event's actual simulation time.
- `src/host/plan-revision-transport.test.ts` contains a targeted persisted-envelope assertion for issue-time swept burn provenance, the ksj failure mode.
- `src/sim/worldline.ts` provides a pure committed-state resolver based on a quantized departure state, Kepler coasts, and committed burn delta-v vectors; `src/sim/worldline.test.ts` pins deterministic and non-planner movement.
- `fort/scripts/verify.sh` exited 0: typecheck and lint passed; 135 tests passed, with 3 configured PostgreSQL integration skips.

## Next actions

1. Harness-owned verification and Warden dispatch.
2. Complete the remaining approved 1ls schema work before close: persist a departure-state stamp once per stream, make the loop own/use the worldline in its production composition, and add the `arrivalRecorded` docked-body handoff event.

## Open risks / questions

- The current repository has no production host composition that constructs a worldline from persisted stream facts. The new resolver is intentionally pure, but it is not yet loop-owned.
- `arrivalRecorded` and the departure-state event need an event-schema/reducer extension; neither should be fabricated as a replay-time ephemerides read.

## Failed attempts

- The first worldline movement pin used a 1 m/s delta-v over one second. Its half-metre displacement rounded away at heliocentric metre scale. The regression now uses a 10 m/s vector so the persisted integer-metre result discriminates the burn.
