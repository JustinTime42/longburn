# Handoff: Forge 2026-08-06T12:27:00-08:00

Model: gpt-5.6-terra

## Plan executed

1. Located the authoritative-loop boundary and preserved `src/sim/` as wall-clock-free.
2. Added a host-only, interval-scheduled tick driver that converts wall-clock samples to elapsed milliseconds.
3. Added deterministic host tests, ran product and fort verification, and recorded the observed fort-verifier failure as follow-up bead `longburn-1gw`.

## Clarifying questions

1. None. The bead specified the required boundary and existing loop API sufficiently.

## State of work

- `longburn-r6g` remains in progress, implemented and committed for harness verification. `HostTickDriver` supplies the missing host half of the continuous loop.
- `longburn-1gw` is open: the fort verifier's existing ShellCheck gate is not green.

## Verified facts

- `src/host/tick-driver.ts:26-113` reads `Date.now()` only in the host adapter, injects clock/timer dependencies for tests, persists elapsed milliseconds through the supplied simulation boundary, coalesces overlapping ticks, and rejects non-integral or backward wall-clock readings.
- `src/host/tick-driver.test.ts:24-109` confirms elapsed wall time reaches `AuthoritativeSimLoop` as a durable `clockAdvanced` event, confirms overlap/error behavior, and covers invalid inputs.
- `eslint.config.mjs:44-89` scopes sim determinism and causal-boundary restrictions to `src/sim/`, matching the documented sim-only constraint while permitting the explicit host boundary.
- `npm run verify` passed: 78 tests passed; 3 PostgreSQL integration tests skipped by existing configuration; lint and strict TypeScript passed.
- `CI=1 fort/scripts/verify.sh --no-emit` passed typecheck, lint, and tests, then failed at its existing ShellCheck gate: missing `bin/fort-init` and warnings in unchanged fort scripts. Follow-up is `longburn-1gw`.

## Next actions

1. Harness verifies and dispatches the required review.
2. Resolve `longburn-1gw` before treating the fort-wide verifier as green.

## Open risks / questions

- A live server still needs to construct the driver with its authoritative event-position resolver and chosen tick interval; this bead intentionally provides the host mechanism only.

## Failed attempts

- Initial `npm run verify` could not find `eslint` because this clean worktree had no installed dependencies. `npm ci --ignore-scripts` restored lockfile dependencies; verification then ran normally.

## Append-only clarification (2026-08-06)

The documented sim-only constraint in this handoff applies to determinism rules only. The `causal-boundary/no-raw-outbound` tripwire is intended to cover all production `src/**` code except `src/sim/causality.ts`; `longburn-itm` restores that scope and pins it with an ESLint configuration test.
