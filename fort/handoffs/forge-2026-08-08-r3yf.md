# Handoff: forge 2026-08-08T01:14:00-08:00

## Plan executed

1. Read the fort law, operating record, Forge protocol, founding specifications, Beads context, and the existing emitted-message exhaustiveness idiom.
2. Add an exhaustive default branch to `CausalStateHost`'s stored-event projection, preserving the intentional no-message variants.
3. Run focused and fort-wide verification, then commit this append-only handoff.

## Clarifying questions

1. None. The bead precisely specifies the established `buildEmittedMessage` idiom and the projection boundary it must guard.

Model: gpt-5.6-terra

## State of work

- `longburn-r3yf` remains `in_progress` pending harness-owned processing. The projection switch now fails TypeScript compilation when a new `SimEvent` variant lacks a projection decision.

## Verified facts

- `projectStoredEvent` has a `default` branch that binds the unhandled variant to `never`, then throws a diagnostic only if untyped persisted data somehow reaches that branch (`src/host/causal-state-host.ts`). This mirrors `buildEmittedMessage`'s exhaustiveness guard.
- `npm run typecheck` passed.
- Focused `npm test -- --run src/host/causal-state-host.test.ts` passed: 1 file, 3 tests.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-r3yf fort/scripts/verify.sh` exited 0: typecheck, lint, shellcheck, and 29 test files passed; 176 tests passed and 4 configured PostgreSQL integration tests skipped.

## Next actions

1. End this Forge session for harness-owned verification and dispatch.

## Open risks / questions

- None.

## Failed attempts

- The worktree initially had no `node_modules`, so `npm run typecheck` could not find `tsc`. `npm ci --offline --ignore-scripts` restored the lockfile-pinned local dependencies without network access; verification then ran normally.
- The first diagnostic tried to access `.type` on the `never` value, which TypeScript rejects. It was adjusted to the existing guarded cast idiom from `buildEmittedMessage`; typecheck then passed.
