# Handoff: Forge 2026-08-04T15:39:43-08:00
Model: GPT-5 (Codex)

## State of work

- `longburn-din.2` remains in progress pending a live-host verification and Warden review. The placeholder PostgreSQL test is replaced by a real, environment-gated integration suite.

## Verified facts

- `test/event-store.postgres.integration.test.mjs` asserts the pre-applied schema, adapter append/read-back order, database append-only triggers, and a 25-run replay-identical property against `PostgresSimulationEventStore`.
- The suite invokes host `psql` only when `LONGBURN_TEST_DATABASE_URL` is present. It does not use a mock or embedded database, and it redacts database URLs from connection failures.
- `npm run verify` passed after the change: lint, strict TypeScript build, and Vitest (17 passed, 3 intentionally skipped live-Postgres tests).
- `node --check test/event-store.postgres.integration.test.mjs` and `git diff --check` passed.

## Next actions

1. In the harness/CI host that can reach the supplied PostgreSQL fixture, run `LONGBURN_TEST_DATABASE_URL=... npm test -- test/event-store.postgres.integration.test.mjs`.
2. Record the live result, then submit the commit for Warden review.
3. Close `longburn-din.2` only after live verification and Warden approval.

## Open risks / questions

- The Forge sandbox cannot reach `127.0.0.1:5433`; the attempted live run failed at connection setup before SQL execution. This is expected sandbox isolation, not adapter verification.

## Failed attempts

- An initial `psql --set` invocation passed the option and variable as separate arguments. `psql --help` confirmed its long form requires `--set=NAME=VALUE`; the suite now uses that form.
