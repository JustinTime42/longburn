# Handoff: forge 2026-08-09T20:57:04-08:00

## Plan executed

1. Add the epoch as immutable stream metadata in both in-memory and PostgreSQL stores.
2. Persist it from Tier 0 creation and reject resume when the supplied epoch differs or legacy metadata is absent.
3. Add migration and contract coverage, then run the fort verifier.

## Clarifying questions

1. None. I treated legacy streams with no reconstructible epoch as unsafe to resume, so Tier 0 now fails closed rather than assigning a guessed epoch.

Model: gpt-5.6-terra

## State of work

- longburn-4khr remains IN_PROGRESS for harness verification and review. Implementation is committed in `8861e88`.

## Verified facts

- Commit `8861e88` persists `epochUtDaysSinceJ2000` through the stream store and PostgreSQL adapter (`src/sim/event-store.ts`).
- Tier 0 creation persists the supplied epoch; resume reads it before resolver composition and rejects a mismatch or missing legacy epoch (`src/sim/tier0-worldline.ts`).
- Migration `db/migrations/0009_simulation_stream_epoch.sql` adds nullable epoch metadata. Existing rows remain unknown and cannot be resumed as Tier 0, preserving determinism.
- `npm run verify` completed with bare observed exit code 0: 245 passed, 5 skipped database integration tests.
- `fort/scripts/emit.sh` recorded the handoff event for `longburn-4khr`.
- Unrequested behavior changes: none.

## Next actions

1. Harness verification and review.

## Open risks / questions

- Applying migration 0009 to an existing database leaves prior streams with NULL epoch metadata. They require an externally established historical anchor before any migration that fills it; this change deliberately does not infer one.

## Failed attempts

- None.
