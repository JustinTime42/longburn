# Handoff: Forge 2026-08-10T13:35:00-08:00

## Plan executed

1. Read the fort law, operational record, Forge protocol, Beads context, founding specifications, approved notification surface, and the prior din.7.4 implementation handoff.
2. Implement the ratified zero-push-subscription fallback as in-app only, including truthful instrumentation of the actual delivered channel.
3. Implement the ratified N4 short-horizon floor: when ordinary selected lead times yield no future warning for a still-revisable burn, schedule exactly one warning at `max(now, deadline - shortestLead)`.
4. Add focused regression coverage, run the bare fort verifier, emit the verification event, commit the implementation, and record this append-only handoff.

## Clarifying questions

1. None. The Overseer's ruling and the amended notification-surface specification resolve both wake-line decisions. I retained the shortest configured lead in the floor warning's stable identity while carrying its explicit immediate delivery instant, so a newly reachable warning cannot collide with an ordinary lead-time warning.

Model: gpt-5.6-terra

## State of work

- `longburn-5kxx` remains `in_progress`, assigned to Orin. Implementation is committed; harness-owned verification, review, and disposition remain pending.

## Verified facts

- Commit `fba951e8cc31cbfafe16b78a1b0ebd1a20e280ca` makes a push-default notification with zero registered subscriptions attempt the in-app gateway, never the email gateway. If no in-app surface is configured or it declines delivery, the queue remains retryable.
- Successful fallback instrumentation records `in-app`, the actual delivered surface, rather than the initially selected `push` preference.
- `deriveLastRevisionWarnings()` now returns exactly one floor warning when every valid configured warning instant is already past but the revision deadline itself remains future. It uses the shortest selected lead and schedules at the explicit `max(now, deadline - shortestLead)` instant. Expired deadlines remain silent.
- Focused notification tests passed: 3 files, 21 tests.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-5kxx fort/scripts/verify.sh` exited 0: typecheck, lint, shellcheck, 251 tests passed across 37 files; 6 PostgreSQL-gated tests in 2 files were skipped because the Forge sandbox has no `LONGBURN_TEST_DATABASE_URL`.
- `fort/scripts/emit.sh` emitted `verify.pass` for `longburn-5kxx` with the observed test tally.

Unrequested behavior changes: none

## Open risks / questions

- The browser/client-shell in-app surface is still owned by din.8. This delivery layer correctly attempts it when composed; absent composition deliberately leaves the notification retryable rather than using email.
- The PostgreSQL-gated notification integration suite did not run in this Forge sandbox. No migrations were applied.

## Failed attempts

- None. The focused tests, typecheck, lint, and full bare verifier were green after the final regression addition.
