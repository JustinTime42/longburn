# Handoff: Forge 2026-08-07T15:26:39-08:00

## Plan executed

1. Read the fort charter, operational record, Forge protocol, Beads context, current bead, and latest relevant Forge handoff.
2. Separate replay-safe structural flight-plan validation from live-only propellant validation.
3. Add the approved regression for a historically accepted revision made infeasible by a later ship-config change, while retaining live-boundary refusal coverage.
4. Run focused tests, typecheck, whitespace validation, the full fort verifier, emit the required event, and commit this append-only handoff.

## Clarifying questions

1. None. The bead contains the Overseer-approved live-only validation design and explicit expected tests.

Model: gpt-5.6-terra

## State of work

- `longburn-kg2` remains in progress pending harness verification and review. Forge implementation and required verifier are green.

## Verified facts

- `src/sim/event-log.ts` now uses an internal replay-safe validator for `planRevisionApplied`; it keeps structure, executed-burn, past-time, and active-overlap checks, but never calls `projectPropellantForBurns` during reducer application.
- The exported `validateFlightPlanRevision` remains the live command-boundary validator used by both direct and inbound paths in `src/sim/loop.ts`, including the propellant projection.
- `src/sim/event-log.test.ts` proves that a 200,000,000 ms accepted burn is sufficient under a prior 5% structural-mass configuration, exhausted under the current `TIER0_SHIP` configuration, and still replays through both direct and persisted replay.
- Focused command passed: `npm test -- --run src/sim/event-log.test.ts src/host/plan-revision-transport.test.ts` (24 tests).
- `npx tsc --noEmit` and `git diff --check` passed.
- Bare command passed: `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-kg2 fort/scripts/verify.sh` exited 0. Typecheck, lint, test, and shellcheck passed: 19 files passed, 1 skipped; 130 tests passed, 3 skipped.
- The verifier emitted `verify.run` and `verify.pass` for `longburn-kg2`.

## Next actions

1. None in the Forge lane. End this session for harness processing.

## Open risks / questions

- The three PostgreSQL integration tests remain intentionally skipped by the existing suite.
- A stream modified by a shell-access actor can now replay its accepted plan without a fuel-consistency rejection. This is the explicitly accepted residual risk in the bead; structural read validation remains tracked by `longburn-ab7`.

## Failed attempts

- None.
