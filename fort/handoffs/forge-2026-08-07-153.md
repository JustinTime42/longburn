## Plan executed

1. Move queued ship-commit time checks, decision-window construction, and position sampling into `AuthoritativeSimLoop`'s serialized critical section.
2. Convert host and command position inputs to thunks, then pin an in-flight host-tick interleave against the later loop state and position.
3. Run the fort verifier, emit its result, commit the implementation, and leave this append-only handoff.

## Clarifying questions

1. None. The bead specified the affected critical section, required regression, and no-expansion boundary.

Model: gpt-5.6-terra

## State of work

- `longburn-153` is implemented and awaits the harness-owned verification and review flow.

## Verified facts

- `AuthoritativeSimLoop` now derives and returns committed decision windows after entering its writer queue, rejects a departure that became past while queued, and evaluates the position thunk at append time.
- `HostTickDriver` passes its existing position thunk through rather than snapshotting it before `advance` queues.
- `src/host/tick-driver.test.ts` blocks a tick append, queues a commitment, changes the command position, then proves the persisted commitment has event time 10 and the later position. It also checks the returned decision windows.
- The REST controller maps the loop's queued departure rejection back to the existing `departure-before-commit` refusal.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge fort/scripts/verify.sh` exited 0: typecheck, lint, 107 tests passed, 3 PostgreSQL integration tests skipped without `LONGBURN_TEST_DATABASE_URL`, and shellcheck passed. `git diff --check` passed.
- Emitted append-only `verify.pass` for `longburn-153`.

## Next actions

1. Harness owns the remaining verification recording and Warden dispatch for `longburn-153`.
2. Close the bead only after the harness review is green.

## Open risks / questions

- None discovered within this bead's scope.

## Failed attempts

- The worktree had no local dependencies. A temporary symlink to the primary checkout's lockfile-pinned `node_modules` enabled verification and was moved recoverably to `/tmp/longburn-153-node_modules-symlink` before commit.
