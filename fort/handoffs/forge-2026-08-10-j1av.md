# Handoff: Forge 2026-08-10T09:51:15-08:00

## Plan executed

1. Read the fort law, operational record, Forge protocol, Beads context, founding specifications, prior j1av handoffs, and the Warden R3 requirement.
2. Preserve per-event arrival-time scheduling and observer-local delivery-sequence compaction, while adding a persisted immutable source-position acknowledgement horizon.
3. Refuse a candidate that a partial projection re-ranks into the compacted sequence prefix but whose durable source position is newer than the persisted horizon.
4. Add the minimal regression, alternating-loss trace, and presented-subset sweep; update the PostgreSQL adapter, migration, documentation, and the append-only R2 correction; run the bare verifier.
5. Commit the implementation, write and commit this handoff, then end for harness-owned verification and review.

## Clarifying questions

1. None. I chose the Warden's Option A because it is specified against the durable event position and keeps the existing arrival-time delivery semantics intact.

Model: gpt-5.6-terra

## State of work

- `longburn-j1av` remains `in_progress`. The R3 remediation is committed; disposition and review are harness-owned.

## Verified facts

- Commit `ac31fd26dcc287f9c91668743c9af160613234c3` adds `sourceGlobalPosition` to acknowledgements and persists `acknowledged_through_position` with PostgreSQL `GREATEST` semantics.
- `EmissionScheduler.run()` now raises `DeliveryProjectionViolation` when a candidate ranks within the compacted sequence prefix but has a source global position beyond that durable acknowledgement horizon. Arrival-time scheduling remains unchanged.
- `src/sim/emission-scheduler.test.ts` covers the one-acknowledged/one-new minimal case, the three-new-message backlog, the passes 2–5 alternating-loss trace, and all non-empty subsets of `[10, 20, 30, 40]`. A shape either emits each new candidate, refuses, or suppresses only the already acknowledged durable identity.
- `src/sim/delivery-cursor.test.ts` verifies the cursor preserves the highest immutable source position across sparse acknowledgements; the DB-gated integration fixture is updated for the new schema and cursor fields.
- The R2 handoff is preserved append-only and ends with a correction of its false count-guard claim.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-j1av fort/scripts/verify.sh` exited 0: typecheck, lint, shellcheck, 248 tests passed across 37 files; 5 database-gated tests in 2 files were skipped because no test database URL was supplied.

## Open risks / questions

- The live PostgreSQL integration path did not run in this Forge sandbox. I did not apply migrations, per Forge protocol; the Mayor must run the documented fresh-database integration leg before merge.
- The existing compaction design still retains the Warden's R2 non-blocking scaling and permanently-undeliverable-candidate risks; this remediation does not expand into those Mayor-owned beads.

## Failed attempts

- None in this round. Focused tests, typecheck, lint, and the full bare verifier passed on the first post-change run.

Unrequested behavior changes: none
