# Handoff: Forge 2026-08-08T18:40:00-08:00

## Plan executed

1. Read the governing fort records, Forge seat protocol, Beads context, Tier 0 specification, causality design, emitted-state catalog, and the latest dependent Forge handoffs.
2. Trace the production outbound composition and add an end-to-end test that wires `EmissionScheduler` through `CausalStateEgress`, `CausalEmissionGate`, and a WebSocket writer.
3. Exercise the virtual 40-day seeded transit stream, liveness, authoritative staleness, independent tick boundaries, a deliberately violating real-transport fixture, and the arrivalRecorded stored-terminal-position regression.
4. Run focused checks and the complete fort verifier, then commit the suite and this append-only handoff.

## Clarifying questions

1. None. The bead's acceptance criteria map directly to the existing Tier 0 transport composition and tests without needing an interface or specification change.

Model: gpt-5.6-terra

## State of work

- `longburn-din.5.4` remains `in_progress` pending harness-owned processing. Commit `0d4c486` adds the end-to-end causality suite.

## Verified facts

- `src/host/causal-state-egress.e2e.test.ts` drives the real Tier 0 outbound composition, not a mocked gate: `EmissionScheduler` supplies candidates to an observer-bound `CausalStateEgress` subscription, which serializes the gate-approved envelopes to the writer.
- The 40-day virtual-clock scenario carries seeded Earth, Moon, mid-transit, and Mars/arrivalRecorded reports to the HQ observer. It asserts every scheduled report reaches the writer, independently verifies the light-time lower bound over the entire emitted stream, and verifies `stalenessMs = emissionTimeMs - eventTimeMs` for each envelope.
- Boundary expectations use a local physical oracle with a literal SI light-speed value, not a production causality helper: `ceil(exact)-1` is blocked and `ceil(exact)` writes through the real WebSocket boundary.
- The dedicated violating fixture sends a three-light-second-early candidate through `CausalStateEgress`; it is refused, writes nothing, records an early-emission incident, and increments the causality counter.
- The named `arrivalRecorded` regression uses a terminal stored position 3.3 light-seconds from HQ while a deliberately disagreeing body resolver would return HQ. The report defers until the stored-position tick, emits with that stored provenance, and never invokes the resolver.
- Focused test, typecheck, lint, and `git diff --check` passed. Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-din.5.4 fort/scripts/verify.sh` exited 0: typecheck and lint passed; 27 test files passed, 166 tests passed, and 3 configured PostgreSQL integration tests skipped; shellcheck passed.

## Next actions

1. End this Forge session for harness processing.

## Open risks / questions

- Tier 0 has no separate HTTP/WebSocket listener shell. This suite therefore covers the existing authoritative outbound composition at its transport boundary; any future server adapter must retain the E3 structural fence and be covered when introduced.

## Failed attempts

- The first typed scheduler adapter exposed `observer-mismatch` as a result type outside the scheduler contract. The adapter now rejects that impossible miswiring explicitly, leaving only the scheduler's gate-result type on the production-shaped path.
