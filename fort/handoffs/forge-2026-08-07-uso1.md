# Handoff: Forge 2026-08-07T19:02:00-08:00

## Plan executed

1. Read the fort charter, operational record, Forge protocol, Beads context, Tier 0 specification, review notes, relevant prior handoffs, and the scheduler, ledger, migration, and composition contracts.
2. Replace global-position watermark delivery with independently scheduled, per-message acknowledgement accounting, retaining only conservative contiguous-prefix compaction.
3. Remove the scheduler's ordered-input and head-of-line requirements; sort simultaneous legal arrivals by `globalPosition`, preserve duplicate-suppression observability, and add regressions for these rules.
4. Update the operational documentation and pre-durability cursor migration, run focused and full Fort verification, then commit this append-only handoff with the implementation.

## Clarifying questions

1. None. The Overseer's ruling supplies the needed delivery semantics. I treated `globalPosition` gaps conservatively: only literal contiguous positions compact into the low watermark, while acknowledgements across a gap remain explicit.

Model: gpt-5.6-terra

## State of work

- `longburn-uso1` remains `in_progress` for the harness-owned verification and dispatch flow. This session does not launch a review or close the bead.

## Verified facts

- `EmissionScheduler.run()` accepts unordered catalog input, independently evaluates every event's causal arrival, and no longer stops after a deferred, malformed, or transport-failed message. Equal legal arrival ticks are emitted in ascending `globalPosition` order.
- `DeliveryCursorStore` is now an acknowledgement ledger: a compacted contiguous low-watermark plus explicit acknowledged messages above gaps. It records only after transport acknowledgement, so restart behavior remains at-least-once.
- The PostgreSQL adapter and `0003_delivery_cursors.sql` supersede the single-row compare-and-advance shape with per-message acknowledgement persistence. This is safe to replace because the prior migration has no production durability data.
- Re-presenting a delivered message remains a structured call to `incrementBelowCursorSuppression`; it does not silently disappear.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-uso1 fort/scripts/verify.sh` exited 0: typecheck, lint, shellcheck, and tests passed. Vitest reported 165 passing tests and 3 configured PostgreSQL integration tests skipped because the Forge sandbox has no database URL.

## Next actions

1. End this session for the harness-owned verification and dispatch flow.

## Open risks / questions

- The PostgreSQL integration suite needs a provisioned `LONGBURN_TEST_DATABASE_URL`; this established sandbox skip leaves the real database statement unexecuted here.

## Failed attempts

- None.
