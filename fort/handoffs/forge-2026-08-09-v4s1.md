# Handoff: forge 2026-08-09T20:45:47-08:00

## Plan executed

1. Read fort law, operational records, the Forge protocol, founding specification and review notes, the bead, the relevant prior handoff, shared event interfaces, and existing inbound writers.
2. Use the reducer's validation-only `commandIssued` transition as one shared dry-run before durable append in both inbound writer paths, covering all three public writers.
3. Add a regression that submits faster-than-light arrivals to each writer and proves no event persists or poisons resume; run focused checks and the bare fort verifier; commit implementation and this handoff.

## Clarifying questions

1. None. I followed the bead's specified reading that reducer application of `commandIssued` is the shared validation boundary and is state-neutral.

Model: gpt-5.6-terra

## State of work

- `longburn-v4s1` remains `in_progress`, pending harness-owned verification and review.

Unrequested behavior changes: none

## Verified facts

- Commit `d11d88f974c957c23aba71d1e4949aff3c68191a` performs the shared pre-append reducer dry-run for inbound plan revisions and trade commands.
- The regression covers plan revision, sell order, and spot-disposition revision. Each rejects an arrival 1 ms before its c-limited minimum; the stream remains empty and `resume()` succeeds.
- `npx vitest run src/sim/inbound-causality.test.ts src/sim/loop.test.ts`: 2 files, 12 tests passed.
- `npx tsc --noEmit`: passed.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-v4s1 fort/scripts/verify.sh --no-emit` exit code: 0. It reported 37 files passed, 2 skipped; 241 tests passed, 5 skipped.

## Next actions

1. Harness verifies and dispatches review.

## Open risks / questions

- None discovered within this bead's scope.

## Failed attempts

- None.
