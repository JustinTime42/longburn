# Handoff: Forge 2026-08-08

## Plan executed

1. Read the fort charter, operational record, Forge seat protocol, Beads context, founding specification, emitted-state catalog, causality design, and the latest emission-layer handoffs.
2. Replace the e2e-only `wiredScheduler` adapter with a production host composition that projects stored events, constructs the scheduler, and binds it to observer-specific causal egress.
3. Cover the stored-event projection and migrate the existing end-to-end causal stream to the production host.
4. Update the host-boundary documentation, run focused checks and the full fort verifier, emit the verification record, and commit this append-only handoff.

## Clarifying questions

1. None. The existing scheduler, egress, and emitted-message contracts were sufficient to make the composition concrete without widening a shared interface.

Model: gpt-5.6-terra

## State of work

- `longburn-m7wr` remains `in_progress` pending harness-owned review processing. This commit supplies the production scheduler-to-egress assembly required by din.8 and kyr.

## Verified facts

- `CausalStateHost` is the sole host-side composition: it maps `StoredEventForEmission` records to catalog-backed `ScheduledEmission`s, schedules them with explicit `SimTimeMs`, and owns the observer-bound `CausalStateEgress` subscription (`src/host/causal-state-host.ts`).
- The adapter rejects egress's `observer-mismatch` result before it reaches `EmissionScheduler`, whose emitter contract deliberately permits only causal gate results.
- The projection takes event position and time from the persisted record. The existing 3.3-light-second `arrivalRecorded` regression now reaches the writer through `CausalStateHost`, proving stored terminal provenance remains load-bearing (`src/host/causal-state-egress.e2e.test.ts`).
- A direct projection suite covers arrival provenance, command-outcome identity, and fail-closed rejection of an outcome without a durable command ID (`src/host/causal-state-host.test.ts`).
- Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-m7wr fort/scripts/verify.sh` exited 0: typecheck, lint, shellcheck, and 28 test files passed; 168 tests passed and four configured PostgreSQL integration tests skipped.

## Next actions

1. End this Forge session for harness processing.

## Open risks / questions

- This module intentionally accepts a raw `WebSocketStateConnection` only at construction, matching the existing egress boundary. kyr's real server adapter must widen the transport fence with or before any new raw outbound surface, as recorded on that bead.

## Failed attempts

- The worktree initially had no local Node dependencies, so the first typecheck could not find `tsc`. `npm ci --ignore-scripts` restored the lockfile-pinned local toolchain; no dependency manifest or lockfile changed.
