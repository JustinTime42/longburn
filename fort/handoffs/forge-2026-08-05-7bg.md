# Handoff: Forge 2026-08-05T10:15:19-08:00

Model: GPT-5 Codex

## State of work

- `longburn-7bg` is implemented in commit `32bc421`, pending the mandatory Warden review and a host PostgreSQL integration run before it may close.

## Verified facts

- `StoredSimEvent` now distinguishes contiguous, one-based `streamSequence` from globally monotone but gapped `globalPosition`; `append` returns a typed stale-sequence conflict.
- The Postgres adapter serializes a stream's appends with `FOR UPDATE`, assigns `stream_sequence` atomically, and the new migration backfills then uniquely constrains `(stream_id, stream_sequence)`.
- `npm run verify` passed: 76 tests passed; 3 Postgres integration tests skipped because `LONGBURN_TEST_DATABASE_URL` is unavailable in the Forge sandbox.
- `CI=1 fort/scripts/verify.sh --no-emit` completed its repository checks but reported pre-existing ShellCheck warnings and a missing `bin/fort-init`; this bead did not modify fort tooling.

## Next actions

1. Apply migrations through the normal runner on the PostgreSQL fixture and run `LONGBURN_TEST_DATABASE_URL=… npm test -- test/event-store.postgres.integration.test.mjs`.
2. Request Warden review for `32bc421^..32bc421`.
3. Close `longburn-7bg` only after the host integration suite and Warden verdict are green; do not merge or push from Forge.

## Open risks / questions

- The real PostgreSQL CTE and upgrade migration need the host-backed integration run; no database is reachable from this sandbox.

## Failed attempts

- Initial verifier invocation lacked installed dependencies (`tsc` was absent); `npm ci --ignore-scripts` restored lockfile-pinned packages without tracked-file changes.
