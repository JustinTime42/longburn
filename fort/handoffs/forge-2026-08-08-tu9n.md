# Handoff: Forge 2026-08-08, longburn-tu9n

## Plan executed

1. Read the charter, operating record, Forge seat protocol, founding specifications, Beads context, and the malformed-projection host path.
2. Quarantine repeat projection-failure incidents by stable stored global position and classify them as data-integrity failures rather than causality failures.
3. Add a second-pass regression, update the operational README, run focused and full verification, emit the verification event, and commit this append-only handoff.

## Clarifying questions

1. None. The bead expressly permits incident de-duplication, and the poison record has the stable global position needed for that boundary.

Model: gpt-5.6-terra

## State of work

- `longburn-tu9n` implementation is complete and remains `in_progress` for harness-owned processing.

## Verified facts

- `CausalStateHost` retains the stable global positions of projection failures for its long-lived observer lifetime. A poison record remains in the per-pass `blocked` result but records its `invalid-envelope` incident only once (`src/host/causal-state-host.ts`).
- Projection failures no longer increment `incrementCausalityFailure`: malformed persisted data is not evidence of an early emission, so it cannot pin or mask the causality alarm (`src/host/causal-state-host.ts`, `src/host/causal-state-host.test.ts`).
- The regression runs the same malformed record a second time and proves one incident total, zero causality increments, and no loss of the subsequent valid delivery (`src/host/causal-state-host.test.ts`).
- `npm test -- --run src/host/causal-state-host.test.ts`: 3 passed.
- `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-tu9n fort/scripts/verify.sh`: passed, 29 files and 176 tests passed; the existing 4 PostgreSQL integration tests remained skipped.
- `git diff --check`: passed. `verify.pass` was emitted for this bead.

## Next actions

1. End this Forge session for harness processing.

## Open risks / questions

- Incident de-duplication is intentionally in-memory for the lifetime of the long-lived observer host. A new host after process restart may record a single renewed incident for an extant poison record; it still cannot alert once per real-tick pass.

## Failed attempts

- The worktree had no local `node_modules`. I temporarily linked the main checkout's existing dependencies for verification, then removed the link; no dependency files are part of the change.
