# Handoff: Forge 2026-08-07T12:29:00-08:00

## Plan executed

1. Inspect the Warden's durability finding against the command transport, event log, reducer, and resume path.
2. Persist every in-flight PlanRevision as `commandIssued`, reconstruct unmatched commands on resume, and link terminal outcomes to the issued command.
3. Move the issue-time replaced-node race input into the reducer-visible applied event, document refusal precedence and the HQ-coordinate placeholder, then run the fort verifier.
4. Commit the implementation and this append-only handoff.

## Clarifying questions

1. None. The approved flight-plan model, Warden finding, and Mayor's round-two scope specified the required durable command record and replay behavior.

Title: Command transport: PlanRevision at c from HQ — light-time solve, arrival scheduling, arrival-time validity, race rule, typed refusal events
Model: gpt-5.6-terra

## State of work

- `longburn-40j.2` remains in progress pending harness-owned verification and review. Round-two implementation commit: `070a557`.

## Verified facts

- `commandIssued` records the command identity, issue and solved-arrival times, HQ and arrival positions, issue-time replacement set, and opaque plan payload before any signal waits in memory.
- `AuthoritativeSimLoop.resume` rebuilds the inbound queue from unmatched `commandIssued` records; terminal applied/refused events carry the command identity.
- The reducer receives the replacement set on command application and enforces an already-started replaced burn on both direct and persisted replay paths.
- `src/host/plan-revision-transport.test.ts` has 5 tests, including restart while a command is in flight; `src/sim/event-log.test.ts` has 12 tests, including both replay paths for the replacement-set conflict.
- Bare `CI=1 fort/scripts/verify.sh --no-emit` passed with exit code 0: 114 tests passed, 3 PostgreSQL integration tests skipped, 18 test files passed, and 1 test file skipped.

## Next actions

1. Harness owns verification recording and Warden dispatch for `longburn-40j.2`.

## Open risks / questions

- The fixed `T0_EARTH_HQ_POSITION_METERS` remains explicitly marked as the heliocentric-origin placeholder until Earth-at-epoch is wired from ephemerides.

## Failed attempts

- The focused transport assertions initially omitted the newly required `commandIssued` entry; updating them to assert the durable issuance record restored the expected event order.

## Unrequested changes

- None.
