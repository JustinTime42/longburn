# Handoff: Forge 2026-08-08

## Plan executed

1. Read the fort records, Forge protocol, governing Tier 0 specifications, bead, and latest relevant handoff.
2. Replaced the non-discriminating propellant-gate assertion with a case where the retired mass-scaled tolerance disagrees with the exact predicate.
3. Restored the missing Tier 0 wall, empty-sequence, remaining-propellant, and safe-integer overflow coverage.
4. Ran the fort verifier and committed the implementation.

## Clarifying questions

1. None.

Model: gpt-5.6-terra

## State of work

- longburn-e0d remains IN_PROGRESS pending harness-owned review. Implementation committed as `d6c4f4d`.

## Verified facts

- `src/sim/mass-cargo.test.ts:113` now proves the predicate is independent of wet mass: a one-millisecond just-over-wall burn is exhausted for both 1 g and 1,000,000 g ships. The pre-hvx tolerance accepted the former but refused the latter.
- `src/sim/mass-cargo.test.ts:132` straddles the authoritative `TIER0_SHIP` strict wall at 193,452,400 ms / 193,452,401 ms and asserts positive/negative remaining-propellant readouts.
- `src/sim/mass-cargo.test.ts:142` covers empty burns; `:146` covers total-duration safe-integer overflow and its RangeError.
- `fort/scripts/verify.sh` exited 0: typecheck and lint passed; Vitest reported 29 files and 183 tests passed, with 4 database-gated tests skipped.

## Next actions

1. Harness-owned review and bead lifecycle handling.

## Open risks / questions

- The new discrimination fixture intentionally relies on IEEE-754 boundary values. Its explanatory comment records the former 1 g versus 1,000,000 g divergence.

## Failed attempts

- I first specified the Tier 0 wall values in seconds while the committed API takes milliseconds. The full verifier caught the resulting fixture error; corrected to 193,452,400 ms / 193,452,401 ms before the green run.
- `npm ci` could not complete inside this sandbox because esbuild's postinstall binary probe received EPERM. I temporarily linked the already-installed matching dependency tree from the main checkout solely to run verification, then removed that untracked symlink.
