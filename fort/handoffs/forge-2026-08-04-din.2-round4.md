# Handoff: Forge 2026-08-04T18:04:00-08:00
Model: GPT-5.6 Codex

## State of work

- `longburn-din.2` remains in progress. The Round 4 live-Postgres test-driver defect is fixed and committed; harness live verification and fresh Warden review remain required before closure.

## Verified facts

- `test/event-store.postgres.integration.test.mjs` now sends parameterized SQL through `psql --file=-` on stdin, which permits `psql -v pN=value` interpolation of the existing `:'pN'` safely quoted variables.
- The driver retains `ON_ERROR_STOP=1`, its psql-only real-database boundary, and database-URL redaction from errors.
- `node --check test/event-store.postgres.integration.test.mjs`, `git diff --check`, and `npm run verify` passed: ESLint, strict TypeScript build, and 17 tests passed. The 3 live tests were skipped with their documented reason because `LONGBURN_TEST_DATABASE_URL` is unset in this Forge sandbox.

## Next actions

1. Harness: run `LONGBURN_TEST_DATABASE_URL=… npm test -- test/event-store.postgres.integration.test.mjs` against the clean PostgreSQL 16 fixture; all three tests must pass.
2. Request fresh Warden review after that live result.
3. Close `longburn-din.2` only after live verification and Warden approval.

## Open risks / questions

- The Forge sandbox cannot access the harness PostgreSQL fixture, so the required live path remains unverified locally.

## Failed attempts

- `psql --command` does not interpolate psql variables even when the process receives `-v name=value`; its SQL reaches PostgreSQL with raw `:'name'` tokens. It must not be reintroduced for parameterized test queries.
