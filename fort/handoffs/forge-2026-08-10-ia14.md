# Handoff: Forge 2026-08-10T13:46:26-08:00

## Plan executed

1. Read the fort law, operational record, Forge protocol, notification product specification, Beads context, prior notification handoffs, and the 5kxx ruling.
2. Extend the push gateway contract with terminal versus retryable failure typing, prune terminal endpoints, and preserve the ratified in-app-only fallback when every endpoint is terminally dead.
3. Pin the accepted mixed-outcome policy, run focused checks and the bare fort verifier, commit the implementation, and record this append-only handoff.

## Clarifying questions

1. None. I read the amended bead and 5kxx ruling together: a retryable push failure leaves its subscription and queue item intact; only when every registered endpoint returns the typed terminal case does delivery take the existing zero-subscription in-app branch.

Model: gpt-5.6-terra

## State of work

- `longburn-ia14` remains `IN_PROGRESS`, pending harness-owned verification, review, and disposition.

Unrequested behavior changes: none

## Verified facts

- Implementation commit: `ea48702e74addcb59ca9d08e1b91d153996d1f55 longburn-ia14: prune terminal push subscriptions`.
- `WebPushGateway.deliver()` now returns a discriminated result: successful delivery, retryable failure, or terminal failure. The terminal case is the only one that invokes `NotificationRouteStore.removePushSubscription()`; `PostgresPushSubscriptionStore` deletes by the persisted endpoint.
- Every typed terminal push outcome is pruned. If all registered subscriptions are terminally dead, the transport uses the same in-app fallback as the zero-subscription branch and never drafts email. Without an in-app surface, the queue remains retryable.
- The transport comment and regression pin the intended mixed outcome: any successful push delivery is sufficient, while the remaining subscriptions are still attempted so stale terminal routes can be pruned.
- Focused `npm test -- --run src/host/notification-transport.test.ts src/host/notification-queue.test.ts` passed: 2 files, 13 tests. `npm run typecheck` and `npm run lint` passed.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-ia14 fort/scripts/verify.sh` observed exit code 0: 37 files / 253 tests passed; 2 PostgreSQL-gated files / 6 tests skipped because this Forge sandbox has no `LONGBURN_TEST_DATABASE_URL`.

## Open risks / questions

- The gateway implementation that maps actual web-push provider responses, including RFC 8030 410 Gone, to the new terminal result is owned by the eventual host composition/provider adapter. This repository currently contains the transport boundary and test doubles, not a provider client.
- The PostgreSQL integration test suite was skipped in this sandbox. The new delete query is unit-covered; no migration changed or was applied.

## Failed attempts

- None.
