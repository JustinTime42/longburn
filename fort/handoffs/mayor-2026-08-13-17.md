# Handoff: Mayor 2026-08-13T21:55-08:00
Model: Claude Opus 5 (1M context)

Scope-fenced session, set by the Overseer at open: **record-keeping only.** Nothing
claimed from `bd ready`, no work started, no merge attempted. Manyhalls is mid-way
through a parity programme (`fortkit-wg8w`) that will land changes in this fort
shortly, and this seat stayed out of its way deliberately.

## State of work

- **longburn-holk** CLOSED. Regent edict `fortkit-52vf.12` (E10): the mayor.sh
  guard hoist at `9860e0d`. Sereth Twicewalked's review returned **ESCALATE per
  charter human gate 4** (`fort/scripts`), with **no blockers** and six findings,
  all non-blocking. The ESCALATE was gate-procedural rather than a defect: gate 4
  reads "Warden + Overseer review", and a read-only seat inside this fort cannot
  verify the Overseer's direction from where it sits. Both halves are now on the
  bead, so the gate is satisfied.
  - **Gate-4 human half recorded**, actor `justin`, the Overseer's words verbatim,
    confirming he directed the widening past E10's own exclusion of this fort. The
    transcription by this seat is disclosed in the comment's first line. This
    closes Sereth's finding 6, which was the whole substance of the ESCALATE.
  - **Finding 1's local half recorded**, append-only under SO 7. The bead's
    description is not edited; the comment is the correction of record.
  - **Nothing was merged.** Sereth's write-up says "then merge"; `9860e0d` was
    already the tip of main, so there was no work behind it.
- **longburn-nogj** FILED (P3, task). Finding 2: after the hoist, an *unmasked*
  caller with an unresolvable repo still gets a bare `exit 1` with no message. That
  is the opaque-failure class E10's own record indicts, surviving one branch over.
  One `printf ... >&2` closes it. Regent lane.
- **longburn-5yc6** FILED (P3, task). Finding 3: the replacement comment in
  `lib/seat-sandbox.sh` says codex's writable roots are "the explicit `--add-dir`
  grants", but `--add-dir` appears nowhere in this fort's `forge.sh` (it occurs
  only in `warden.sh`, as a claude flag). The substance survives, the mechanism is
  misnamed, and it is misnamed inside the very sentence written to close shared
  comment drift. Mechanism-neutral wording, all four copies in one change per
  `fortkit-29e9`. Regent lane, cross-fort.
- **longburn-2x6w** FILED (P3, task). Finding 4: the probe's positive control
  cannot distinguish "reaches the launcher" from "dies two lines later", because
  under a rendering whose fallback points at a nonexistent path the unmasked run
  also dies at the `cd`. The always-refuses regression was excluded by the
  byte-identical guard body, not by the control. Bead carries the hard constraint
  that the probe must stay safe by construction: never a `session.start` in a live
  stream, never a spawned Mayor.
- **longburn-70v9** FILED (P2, task). Finding 5: **no correction is owed.** Calder's
  `incident.corrected` landed 16 seconds behind the misrouted incident, named its
  own cause, and honoured SO 7. What is missing is the mechanism. `emit.sh`
  resolves its target stream from cwd, so a civ seat working across settlements
  writes into whichever fort it is standing in. This is the sixth bite of the cwd
  class, and the first arriving from above; the fort answered the seat-side five
  with `longburn-ifih`. Filed here because this is the settlement that took the
  misattribution, and covenant 4.5 gives this fort's Warden a review right that is
  unexercisable against an unrecorded finding.

Finding 1's **upward** half needed nothing from this seat: Emrith appended the SO 7
correction to `fortkit-52vf.12` and `fortkit-px7e` in the capital this evening.

## Verified facts

- `longburn-holk` closed with full disposition in the close reason; four beads
  created (`bd create`, confirmed by their creation lines).
- Events emitted this session, all in `fort/events/events-2026-08-13.jsonl`:
  `session.start`, `overseer.decision` (gate-4 half, target `longburn-holk`),
  `bead.filed` (the four), `push` (git), `push` (dolt).
- Records commit **`c069aa6`**, path-scoped: `.beads/interactions.jsonl` and
  `fort/events/events-2026-08-13.jsonl` only. Follow-up commit **`a72daf6`** carries
  the git push event's own line. This handoff rides the commit that added it
  (`git log --diff-filter=A -1 -- fort/handoffs/mayor-2026-08-13-17.md`); it cannot
  name its own hash from inside itself.
- **git push (Overseer-authorized): `3e9946f..c069aa6`, one commit.** The
  authorization named "24 commits ahead"; that figure was stale. `git fetch` then
  `rev-list --count origin/main..main` returned **1**, and `origin/main` already sat
  at `3e9946f`, which carries `9860e0d` and the E10 review stream. Reported before
  pushing, and recorded in the push event so the discrepancy is not lost.
- **bd dolt push (Overseer-authorized):** reported "Push complete";
  `git ls-remote origin 'refs/dolt/*'` shows `refs/dolt/data` at
  `75f35ba4bec8cffd9ec1e9c3c35cd159b8330d84`. That is the full extent of what was
  observed. `bd dolt status` reports only the embedded engine and data path, so
  there is no local ahead/behind readout to check the sync against. SO 6 applies:
  treat "reported complete" as weaker evidence than a verified remote state.
- Working tree clean at close apart from `docs/specs/.market-model-v0.1.md.kate-swp`,
  an editor swap file, left untracked and untouched.

Sereth's own verification, reproduced here because it is what makes the close safe:
bare `CI=1 fort/scripts/verify.sh --no-emit` exit 0, twice, unpiped sentinel, **255
passed / 6 skipped, 37 test files**; the lib change proven comment-only (non-comment
diff lines = zero); the mayor.sh change proven a pure move (the two `REPO=`/`cd`
lines removed and re-added character-identical, guard body untouched). This seat did
not re-run any of it.

## Next actions

1. **`a72daf6` and the handoff commit are unpushed** unless the closing push in this
   session's last exchange covered them. Check `git rev-list --count origin/main..main`
   before assuming either way; do not assume from this file.
2. **Expect incoming changes from Manyhalls** (`fortkit-wg8w`, parity programme).
   Pull before starting anything, and read whatever it lands in `fort/` before
   trusting this handoff's picture of the fort's machinery.
3. The four new beads are **all Regent or capital lane** except in review: none can
   be executed from a seat here, because `fort/scripts/` is kernel read-only WHOLE
   since E2b. They belong in the next Regent batch alongside `longburn-ifih`,
   `longburn-3195`, and `longburn-gr8f`. `longburn-70v9` is the P2 of the set.
4. The mill's real queue is untouched and unchanged from session 16's handoff:
   9j0 r2 and 9hi7 r2 dispatch, din.8.1 r2 verdict, and the Overseer decision queue
   (**vegv** still gates din.8.2). Read `fort/handoffs/mayor-2026-08-11-16.md`, not
   this file, for that picture.

## Open risks / questions

- **`longburn-70v9` is the one to actually build.** Five prose failures bought this
  fort a merge guard; the sixth says the same weakness exists in the emit path, and
  the only thing that caught it was a civ seat noticing its own mistake within 16
  seconds. The next drift may not be noticed by its author.
- **The gate-4 human half is now a transcription.** It is Justin's words, confirmed
  verbatim before writing, and the transcription is disclosed in the comment itself.
  A stricter form exists if the fort ever wants it: the Overseer commenting under his
  own hand, or a signed line in a file only he writes. Worth a decision before the
  next gate-4 change, not after.
- Finding 1's shape is worth remembering as a class: a confident frontier model
  writing its own record produced a measurement that was true of the probe and not
  of the artifact. The disclosure was one paragraph below the claim, so nothing was
  hidden. The failure was in what a reader would carry away, and the fix was a
  caveat that travels with the claim.

## Failed attempts

- **Item 1 could not be executed as given.** The Overseer's sign-off arrived as an
  unfilled template, `[EDIT THIS — e.g.: ...]`, with the instruction to record it
  verbatim. Recording the placeholder would have put "EDIT THIS" on a permanent gate-4
  attestation; filling the example in as his signed words would have manufactured the
  human half of a human gate, which is precisely what finding 6 objects to. The seat
  stopped, did everything not depending on the answer first, then asked. He confirmed
  the example text as written, and it was recorded. **Do this again next time.** The
  cost of asking was one turn.
- `bd dolt status` was tried as a sync verification and gives nothing usable for it:
  no remote, no ahead/behind, only the engine mode and data path. `git ls-remote
  origin 'refs/dolt/*'` at least proves the ref exists and gives a sha to compare
  against later.
