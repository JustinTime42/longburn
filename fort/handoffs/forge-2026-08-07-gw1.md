# Handoff: Forge 2026-08-07T17:42:00-08:00

## Plan executed

1. Read the charter, operational record, Forge protocol, Beads context, emitted-state catalog, causality design, founding specs, and relevant prior Forge handoff.
2. Consolidate the gate output and catalog envelope into one `EmittedMessage`, with gate-owned staleness computation.
3. Make `class` and `payload` a correlated discriminated union, carry `observerId` as envelope data, and use global log order for equal-time aggregate ties.
4. Add focused regression coverage, run the full fort verifier, and commit path-scoped work.

## Clarifying questions

1. None. The bead, Warden findings, and approved catalog resolved the interface and scope.

Model: gpt-5.6-terra

## State of work

- `longburn-gw1` remains in progress pending the harness-owned verification and review flow. Implementation commit `60c4434` completes f1, f2, f3, and f5 without adding E2 scheduling or E3 transport behavior.

## Verified facts

- `src/sim/emitted-message.ts` now owns the sole `EmittedMessage` type as a mapped discriminated union. Narrowing `class: "commandOutcomeReport"` narrows the payload, and the tests access a refused outcome reason with no assertion cast.
- `CausalEmissionGate` emits that sole envelope and computes `stalenessMs` only at its gate boundary. The candidate contains provenance and the observer worldline, while the emitted envelope receives its sampled observer position and computed staleness.
- `observerId` is an explicit, validated envelope field, available to E2's per-observer cursor without parsing `messageId`.
- `youngestStoredEmissionEvent` resolves equal event times by `globalPosition`, not per-stream `streamSequence`; the regression test selects the greater global position even when its stream sequence is lower.
- Bare verifier command `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-gw1 fort/scripts/verify.sh` exited 0: typecheck, lint, tests, and shellcheck passed; 143 tests passed and 3 configured PostgreSQL integration tests skipped.

## Next actions

1. None in the Forge lane. End this session for harness processing.

## Open risks / questions

- The generic `OutboundEvent<T>` helper remains exported for the standalone causality solver, but the gate no longer accepts or emits it. Future consumers must use the concrete envelope candidate/output boundary.

## Correction appended 2026-08-07

- `OutboundEvent<T>` was not retained by any standalone causality-solver consumer: repository search found no references outside its declaration. The stated retention reason above was unsupported. `longburn-cav` removes the dead type; the concrete envelope boundary remains the only outbound contract.

## Failed attempts

- The first focused sim-clock test placed its observer at the fixture's original event position after an earlier test intentionally mutated that stored position. It was an order-dependent fixture error, not a gate defect; the test now uses the event record's current authoritative position.
