# Handoff: Forge 2026-08-07T12:45:00-08:00

## Plan executed

1. Read the charter, operational memory, Forge seat protocol, approved flight-plan model, current bead, and the preceding command-transport handoff.
2. Add the reducer-owned inbound light-cone assertion for durable `commandIssued` records.
3. Add deterministic property coverage for inbound causality, revision-race replay, immutability, and per-node quantization, plus the assigned restart and serialization regressions.
4. Run the fort verifier, commit the implementation and this append-only handoff.

## Clarifying questions

1. None. The approved model and explicit Warden scope additions specified the behavior precisely.

Title: Inbound-causality verifier suite + revision-race replay property tests
Model: gpt-5.6-terra

## State of work

- `longburn-40j.5` remains in progress because the required fort verifier cannot reach a green result in this sandbox.

## Changes made

- The shared reducer now treats every durable `commandIssued` record as an inbound light-cone claim and rejects an arrival earlier than HQ-to-arrival-position light time.
- Added generated inbound boundary tests over epochs and three-axis geometries, asserting direct and persisted replay agree.
- Added generated revision-race replay streams containing ship events, per-node quantization round trips, and reachable inbound-command immutability sequences.
- Added restart cases for non-empty replacement reconstruction, stopping before rebuilt arrival, post-restart refusal, and issuing while an advance is in flight.

## Verification

- `git diff --check` passed.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-40j.5 fort/scripts/verify.sh` failed at typecheck with exit 127: `tsc: command not found`.
- `npm ci --offline --cache /home/justin/.npm --no-audit --no-fund` could read cached packages but failed in `esbuild` postinstall with `EPERM`; npm left `.bin` symlinks whose target package files are absent. The verifier therefore could not reach lint, tests, or shellcheck.

## Surprise / blocker

- This worktree began without usable `node_modules`. The sandbox prevents a cached dependency install from completing, so full verification is externally blocked rather than a code-test failure.

## Unrequested changes

- None.
