# Handoff: Forge 2026-08-07 round 2

## Plan executed

1. Replace the separate live-loop and persisted-replay ship reducers with one shared event reducer.
2. Make `shipOrderCommitted` carry its two scheduled decision windows, so command commitment is one durable append.
3. Refuse empty identifiers and an all-zero-duration maneuver at the REST command boundary before persistence; extend reducer and command tests; run all verifiers and commit the result.

## Clarifying questions

1. None. The Mayor's R2 scope specified the event shape, validation requirements, and excluded findings.

Model: gpt-5.6-terra

## State of work

- `longburn-din.4` remains in progress pending the harness-owned verification and review flow.
- R2 fixes are committed with this handoff.

## Verified facts

- `SimEventReducer` is the single ship-event reducer used by `replaySegment` and `AuthoritativeSimLoop`, so both paths validate and reduce the same event shapes.
- `shipOrderCommitted` now contains the scheduled re-target and arrival-profile decision windows. A committed order is one store append, followed by local reduction; no partial decision append is possible.
- `ShipOrderRestController` throws `ShipOrderCommandRefusal` before any append for empty order IDs, empty destination IDs, and all-zero total durations. Tests assert the store remains empty for every refusal.
- The retained full-continuum test covers zero acceleration and coast with a nonzero impulsive deceleration burn. Exact phase-boundary behavior remains unchanged.
- `README.md` records the atomic event contract and refusal behavior.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge fort/scripts/verify.sh` passed with exit code 0: typecheck, lint, tests, and shellcheck all passed. Tests: 104 passed; 3 configured PostgreSQL integration tests skipped because no database URL is configured.

## Surprises

- `npm test -- --runInBand` is not a supported Vitest option in this repository and failed before running tests. A plain `npm test` then passed; no source change was required.

## Next actions

1. Harness owns verification recording and Warden dispatch for `longburn-din.4`.
2. After the harness review is green, close the bead through the normal Beads workflow.
