# Handoff: Forge 2026-08-08

## Plan executed

1. Inspect the command-identity writers, resume reconstruction, the emitted-state catalog, and the relevant transport tests.
2. Reserve a `local-` prefix for direct local plan-revision outcomes and document the shared-namespace invariant at both mint sites.
3. Add a mixed-origin restart regression, run focused tests and the fort verifier, then commit the implementation and this append-only handoff.

## Clarifying questions

1. None. The bead specifies the `local-` origin prefix and the existing command-ID contract permits it.

Model: gpt-5.6-terra

## State of work

- `longburn-xo9b` remains `in_progress`, awaiting harness-owned review and closure.

## Verified facts

- Direct `applyPlanRevision` and `refusePlanRevision` outcomes now mint `local-${streamSequence + 1}`. Their comments state the shared durable command-ID namespace and why `resume()` requires distinct origins (`src/sim/loop.ts`).
- A regression creates a caller-supplied inbound `command-2`, follows with a local sequence-2 outcome, resumes twice, and confirms the inbound command remains pending and applies at its causal boundary (`src/host/plan-revision-transport.test.ts`).
- `npm test -- --run src/host/plan-revision-transport.test.ts src/sim/event-log.test.ts` passed: 2 files, 28 tests.
- `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-xo9b fort/scripts/verify.sh` exited 0: typecheck, lint, 29 test files passed (177 tests), with 4 configured PostgreSQL integration tests skipped.
- Implementation commit: `f854d60 longburn-xo9b: reserve local command identity namespace`.

## Next actions

1. End this Forge session for harness processing.

## Open risks / questions

- Existing persisted local outcomes with the former `command-` prefix are immutable historical records. The prefix protects all newly written local outcomes; Kyr must preserve the inbound `command-` namespace when it accepts caller identities.

## Failed attempts

- The focused test command initially could not find Vitest because this worktree lacked its local toolchain. `npm ci --ignore-scripts` restored lockfile-pinned dependencies without changing tracked manifests.
