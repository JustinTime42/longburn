# Handoff: Forge 2026-08-07T17:53:00-08:00

## Plan executed

1. Read the fort charter, operational record, Forge protocol, Beads context, founding/review specs, and the relevant gw1 handoff.
2. Make the causal gate's successful envelope type only the four constructible catalog classes, and make malformed final envelopes report `invalid-envelope`.
3. Move observer-local zero-staleness validation to the gate's final-envelope validator, remove the dead `OutboundEvent` alias, and append its historical-record correction.
4. Add targeted regressions, run the full fort verifier, then commit the scoped implementation and handoff.

## Clarifying questions

1. None. The bead precisely specified the contract changes and the existing catalog resolved the constructible-class boundary.

Model: gpt-5.6-terra

## State of work

- `longburn-cav` remains in progress pending the harness-owned verification and review flow. This commit completes f1, f2, f3, f4, and f8 without adding E2 scheduling or E3 transport behavior.

## Verified facts

- `EmittableMessage` is exported as `Extract<EmittedMessage, { class: EmittableMessageClass }>`; the gate's transport callback, internal message, and validator return all use it (`src/sim/emitted-message.ts`, `src/sim/causality.ts`).
- The gate reports malformed payloads, empty observer IDs, and reserved classes as `invalid-envelope`, fails closed, records an incident, and increments the causality failure counter (`src/sim/causality.test.ts`).
- `buildEmittedMessage` no longer duplicates observer-local staleness validation; the final gate validation owns it. Its class switch has an unreachable default throw and a regression test (`src/sim/emitted-message.ts`, `src/sim/emitted-message.test.ts`).
- `OutboundEvent<T>` was removed after repository search found no uses outside its declaration. The correction was appended, not substituted, in `fort/handoffs/forge-2026-08-07-gw1.md`.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-cav fort/scripts/verify.sh` exited 0: typecheck and lint passed; 145 tests passed with 3 configured PostgreSQL integration skips; shellcheck passed.

## Next actions

1. None in the Forge lane. End this session for harness processing.

## Open risks / questions

- `invalid-envelope` is intentionally a gate result for every non-causality exception during final envelope construction. E2/E3 should branch on this typed classification rather than treating malformed envelopes as retryable causality failures.

## Failed attempts

- Calling `fort/scripts/emit.sh --help` returned a shell argument error because the script accepts only positional event arguments and has no help branch. No repository state was changed by that probe.
