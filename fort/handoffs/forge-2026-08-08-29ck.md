# Handoff: Forge 2026-08-08
Model: gpt-5.6-terra

## Plan executed

1. Trace the live propellant gate and its frozen integer ceiling.
2. Convert each committed delta-v vector to an upward-rounded, full-throttle-equivalent duration using integer arithmetic.
3. Preserve the existing live-only acceptance boundary, add regression coverage, run the fort verifier, and commit the handoff.

## Clarifying questions

1. None. The Overseer ruling and bead prescribe the billing unit, rounding direction, and existing ceiling to retain.

## State of work

- `longburn-29ck` remains IN_PROGRESS pending the harness-owned verification and review flow. This commit bills and gates propellant by committed delta-v rather than scheduled firing duration.

## Verified facts

- `src/sim/mass-cargo.ts` derives the conservative `ceil(|dv|)` magnitude in bigint space, then ceiling-divides it by Tier 0's integer acceleration to obtain billable milliseconds. The acceptance predicate still only sums integers against the frozen per-ship ceiling.
- `src/sim/event-log.ts` supplies every executed and pending node's committed vector to the live-only propellant projection; replay remains free of configuration-dependent propellant validation.
- `src/sim/mass-cargo.test.ts` covers throttled billing, non-axial upward rounding, frozen-wall boundaries, and overflow.
- `src/sim/event-log.test.ts` confirms a schedule slot above the frozen ceiling is accepted when its committed delta-v costs one equivalent millisecond. `src/host/plan-revision-transport.test.ts` retains full-throttle over-budget and cumulative-budget transport coverage.
- `fort/scripts/verify.sh` passed: typecheck, lint, 190 tests passed, and 4 PostgreSQL integration tests skipped as configured.

## Next actions

1. Harness runs the assigned review and advances the bead according to its lane.

## Open risks / questions

- The approved spec write-back for flight-plan-model §4 and trajectory-subsystem §3 belongs with `longburn-j2k9`, as directed by this bead.

## Failed attempts

- Worktree-local `npm ci` failed because its `esbuild` postinstall binary cannot execute under this sandbox (`EPERM`), the known worktree install issue. Verification used the repository checkout's compatible dependencies through ignored local links; no dependency or lockfile changed.
