# Handoff: Mayor 2026-08-11T18:40-08:00
Model: Claude Opus 5 (1M context)

## State of work

- **longburn-wtx7** (epic, OPEN) — Migrate Farlantern to the facts ledger. The migration proper is landed on main; four follow-up children remain.
- **longburn-wtx7.1** CLOSED — machinery installed at `6a29ff7`: `fort/memory/{facts/,consolidate-memory.mjs,memory-lint.mjs}`, both generators byte-identical to fortkit `templates/fort/memory/` at `a71ab55`, plus an explicit `fort/memory/index.db` gitignore line.
- **longburn-wtx7.2** CLOSED — 29 bullets migrated to 31 facts at `1a091a9`, with one annal for the episodic remainder of the old file's last bullet.
- **longburn-wtx7.3** CLOSED — charter Memory section, gate-4 gloss, and the mayor/forge session-start lines repointed at `5796245`; `charter.amended` emitted; Overseer's prior approval recorded before the edit and the gate-4 co-sign recorded after.
- **longburn-wtx7.4** CLOSED — CLAUDE.md/AGENTS.md repointed, `fort/remember.md` reduced to a pointer stub, `memory-lint` wired as verifier stage 1, `current.md` generated, all at `0b7fdab`.
- **longburn-wtx7.5** CLOSED — four-part verification recorded, Warden findings dispositioned at `ce36996`, merged to main at `3e41022`.
- **longburn-wtx7.6** OPEN (P2) — the three launcher prompts still name the retired path. Kernel-RO to every seat here; Regent or unmasked Overseer work.
- **longburn-wtx7.7** OPEN (P2) — no bead corpus and no interactions corpus in the memory index.
- **longburn-wtx7.8** OPEN (P1) — no regeneration cadence for `current.md`, and the frozen artifact is the injected one.
- **longburn-wtx7.9** OPEN (P2) — five capital-facing findings to carry to fortkit-xgul.7.
- **longburn-3195** OPEN (P1, new) — masked seats cannot git-checkout any change to `fort/scripts/verify.sh`. Exposed by this migration's own merge.

## Verified facts

- main @ `f045f5e`, fast-forwarded from `70b3fd1`; ancestry asserted with `merge-base --is-ancestor` before the ref moved.
- Post-merge verifier from the main checkout, **bare and unpiped**: exit 0. vitest RUN path `/home/justin/dev/longburn`, read before the result was trusted. 37 test files passed / 2 skipped; 255 tests passed / 6 skipped. `memory-lint` is stage 1 and is named in both the `verify.run` and `verify.pass` payloads.
- Cover check: 29 of 29 top-level bullet line numbers from `d194384:fort/remember.md` (lines 3-19, 23-29, 31, 43-46) resolve to a fact's `provenance.source`. Zero uncovered. The Warden reproduced this independently rather than reading it off the bead, and confirmed `d194384:fort/remember.md` is byte-identical to the version that was live.
- Count reconciliation: 29 bullets in, 31 facts out. 29 − 1 (episodic remainder to `fort/annals/session-15-delta-2026-08-10.md`) + 3 (the `:46` split) = 31.
- `memory-lint` exit 0. Core budgets at close: shared floor `[all]` 2 facts / 53 lines; mayor 8 / 176; forge 2 / 53; warden 2 / 53. Cap is 30 facts / 300 lines per seat.
- `memory-lint` is not vacuous: a planted fact with a mismatched key, empty scope, empty body, empty source, `origin: untrusted` at `tier: core`, and an unresolvable `superseded-by` produced six named failures and exit 1. The Warden ran its own independent control on the unreadable-facts-dir path.
- `current.md` regenerates byte-identically from fixed inputs (`cmp` clean) and discloses four index gaps rather than narrowing silently.
- Warden verdict on `longburn-wtx7`, round 1: **ESCALATE for the gate-4 co-sign only; zero blockers; seven findings; recommends the merge.** Recorded as a bead comment at 2026-08-12 02:05 by sereth (opus).
- Gate-4 co-sign received from the Overseer 2026-08-11 after reading the constitution diff; emitted as `overseer.decision`.
- Events emitted this session: `charter.amended`, `overseer.decision`, `merge`, plus the verifier's own `verify.run`/`verify.pass` pairs and the Warden's `review.verdict`.

## Next actions

1. **Push is unrequested and unapproved.** main is 12 commits ahead of `origin/main`: this session's 7 plus 5 that predate it. Ask, name them, and only then push.
2. **longburn-wtx7.8 (P1)** — give `current.md` a regeneration cadence. Session-start regeneration is the cheap half; fortkit has `.service`/`.timer` proposals to copy for the nightly half. Until then the injected view embeds the 2026-08-10 handoff's stale "Next actions".
3. **longburn-3195 (P1)** — the in-mask verify.sh checkout defect. Needs the Regent, since `lib/seat-sandbox.sh` is kernel-RO. It will recur on every verifier bead.
4. **longburn-wtx7.9** — carry the five capital-facing findings to fortkit-xgul.7. The most load-bearing is item 1: widening memory-lint's retired-reference grep with a plain substring match would go red on the dated amendment notes standing order 7 requires.
5. Resume the pre-existing mill: din.8.1 r2 review disposition, 9j0 r2, and the Overseer queue recorded in the session-15 annal (vegv, hfku, fs2n, 6mmd) — read that annal as a dated record, not a work list.

## Open risks / questions

- **Spec acceptance 10.6 is not met and this is deliberate.** "Zero live references to `fort/remember.md` outside git history" still fails on the three launchers (wtx7.6). The stub keeps them resolving. The other half of 10.6 needed no work: `bd memories` was already empty here.
- **The distilled view's Open work section reads "0 of 0 open beads shown"** because this fort has no `.beads/issues.jsonl` (wtx7.7). A reader could misread that as "no open beads" rather than "no bead corpus indexed"; the line does point at `bd ready`.
- **Both amended charter passages still contain the string `fort/remember.md`**, inside dated amendment notes, because standing order 7 amends by extension. This is correct here and is a hazard for the capital's planned lint widening (wtx7.9 item 1).
- **The memory gate can be switched off with a green build.** Truncating `fort/memory/memory-lint.mjs` to zero bytes passes, because node exits 0, and that file sits under a path the charter calls seat-writable while the verifier that runs it is gate-4 constitution (Warden finding 5, wtx7.9 item 4). Residual control is the reviewed path-scoped commit.
- Nothing detects drift of our two generator copies against the fortkit templates after copy time.

## Failed attempts

- **`git merge --ff-only` into main failed twice from inside the mask**, and so did switching branches in either direction, because `fort/scripts/` is a read-only directory bind: git replaces files by unlinking them, and `verify.sh` had changed on the branch. Error: `unable to unlink old 'fort/scripts/verify.sh': Read-only file system`. Do not retry the plain merge on a verifier bead; it cannot work. The forward-only workaround is recorded on longburn-3195 and in the fact `verify-sh-is-not-checkout-able-in-mask`, and it only applies when the worktree copy already equals the target.
- **The Edit tool fails with EROFS on `fort/scripts/verify.sh`** for the same reason: it writes a sibling temp file before renaming, and the directory is read-only. An in-place rewrite works (`python3` `open(path, "w")`, or a shell redirect onto the existing file). `sed -i` will fail.
- The first `git switch main` aborted on uncommitted event-stream appends. Commit the day's events path-scoped before switching, per the seat's own close protocol.
