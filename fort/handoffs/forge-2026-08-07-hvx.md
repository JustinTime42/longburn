# Handoff: Forge 2026-08-07 longburn-hvx

## Plan executed

1. Trace the arrival-time fuel gate through the mass-cargo projection used by the shared reducer.
2. Replace its per-node exponential/tolerance acceptance predicate with exact committed-duration accumulation and a strict viability-wall comparison.
3. Preserve per-node readouts, add a multi-node wall-boundary test, run the permitted verifier, and commit the implementation plus this handoff.

## Clarifying questions

1. None. The bead prescribed a local replacement and the existing mass-cargo interface already supplied the viability wall and quantized commitment.

Model: gpt-5.6-terra

## State of work

- `longburn-hvx` remains in progress, implemented and ready for the harness-owned continuation.

## Verified facts

- Commit `76fca72` sums validated `burnDurationMs` values as safe integers, derives one total delta-v, and accepts only when it is strictly below `viabilityWallDeltaV(ship)` in `src/sim/mass-cargo.ts`.
- The `8 * Number.EPSILON * wetMassGrams` tolerance and the exponential-derived acceptance decision are removed. Per-node mass and propellant readouts still use the existing sequential rocket-equation projection after the decision.
- `src/sim/mass-cargo.test.ts` covers a two-node 693 ms aggregate below the ln(2) wall and a 694 ms aggregate above it, proving the decision follows the exact integer sum.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge fort/scripts/verify.sh` exited 0: typecheck, lint, 129 tests passed, 3 PostgreSQL integration tests skipped without `LONGBURN_TEST_DATABASE_URL`, and shellcheck passed.
- `git diff --check` passed before the implementation commit.

## Next actions

1. Harness owns the remaining verification and dispatch flow.

## Open risks / questions

- `viabilityWallDeltaV(ship)` remains the established logarithmic wall required by the mass-cargo contract; the removed replay-time decision branch no longer depends on exponential node projections or a mass-scaled epsilon bound.

## Failed attempts

- None.
