# Handoff: Mayor 2026-08-04 (sixth session of the day — short: warden.sh diagnosis, then Overseer-requested restart for new configs)
Model: Claude Fable 5 (Vardis Slowfathom)

## State of work

- **longburn-aak (NEW, P1, open)**: The session's find. The two silent warden.sh failures on din.1 r3 are diagnosed: `fort/scripts/lib/seat-sandbox.sh:90` reads `"$TMPDIR"` unguarded; warden.sh runs `set -euo pipefail`, so a launch context without TMPDIR dies while sourcing the lib — before the claude launch, before tee creates the log, before any emit. Reproduced foreground this session with the WARDEN_SMOKE=1 probe: output was exactly `seat-sandbox.sh: line 90: TMPDIR: unbound variable`, exit 1. Fix is one token (`"${TMPDIR:-}"`; line 95's `[ -n "$w" ]` guard already skips empties). Constitution-tier file (gate 4), so filed for the Overseer to apply, full detail + acceptance criteria on the bead.
- **longburn-din.1 (in_progress, unchanged)**: still awaiting Warden r3, blocked behind aak. Round 3 work `fc18d2c` harness-green in worktree din.1. Sereth's r2: "I expect to approve the next round."
- **longburn-din.2 (in_progress, unchanged)**: Forge round 3 not dispatched this session; scope on the bead (psql `:'var'` parameterization at src/sim/event-store.ts:114, schema assertion shape, property cascade).
- **longburn-abt (open, HUMAN)**: `.claude-flow/` and `.claude/proven-config.json` were still present at session start; my `rm` was denied again (third denial across sessions — this is the Overseer's lane now). settings.json itself remains clean (verified last session). The restart-then-watch test has still never actually started because the junk paths have never been deleted post-fix.

## Verified facts

- `WARDEN_SMOKE=1 fort/scripts/warden.sh longburn-din.1 'main...bead/din.1' /home/justin/dev/longburn-worktrees/din.1 opus` → stderr `fort/scripts/lib/seat-sandbox.sh: line 90: TMPDIR: unbound variable`, exit 1, run foreground this session. No log files created (`/tmp/warden-din.1.log{,.err}` absent before and after).
- `bd show` confirms din.1 and din.2 in_progress, assignee orin. 20 issues open incl. aak; 9 were ready at session start.
- Both abt junk paths existed at session start (ls confirmed); deletion denied in-session; NOT deleted at wrap.
- Postgres container `longburn-din2-pg` (127.0.0.1:5433): not probed this session, presumed running; needed for din.2 round 3.

## Next actions (in order)

1. **Overseer applies aak** (one-token edit to seat-sandbox.sh:90) — it gates the whole review lane.
2. **Overseer deletes** `~/dev/longburn/.claude-flow/` and `~/dev/longburn/.claude/proven-config.json` (or approves the Mayor's rm) so the abt watch can finally start.
3. **Re-run the smoke probe** (same command as above) to confirm the launch survives past the lib, then relaunch the real review: `fort/scripts/warden.sh longburn-din.1 'main...bead/din.1' /home/justin/dev/longburn-worktrees/din.1 opus`. On approve: merge `--no-ff`, verify, close din.1 → unblocks din.3.
4. **din.2: dispatch Forge round 3** (`fort/scripts/forge.sh longburn-din.2`), then harness re-runs the live suite against the container, then Warden review.
5. After din.1 merges: din.3 unblocks. After din.2 merges: din.4/din.5/din.6/din.8 unblock (din.5 also needs 3n9). qp0 ready for a free Forge slot.

## Open risks / questions

- The TMPDIR fix explains the crash, but warden.sh has STILL never completed end-to-end here — after the fix lands, treat the next smoke run as the real validation, and only then the review. There may be a second fault behind the first.
- forge.sh sources the same lib; it worked (din.2 r2), presumably because TMPDIR was set in that launch context or the crash path differs. Worth one glance when applying aak — the fix protects every consumer either way.
- New configs landing at this restart (the Overseer's stated reason for the wrap) — next session should diff-check `fort/remember.md`/charter for changes before acting on this handoff's assumptions.
- If the abt junk paths regenerate after deletion + restart, the settings flags weren't the only generator: file a fresh diagnosis bead, don't reuse abt.
- 8zw (secure the name) still deferred; hosting + notification-transport defaults (din.11/din.7) remain Mayor proposals, not rulings.

## Failed attempts

- `rm -rf .claude-flow` + `rm -f .claude/proven-config.json`: denied again this session (third time). Stop attempting from the Mayor seat; it needs the Overseer's hand or an explicit approval.
- (Inherited, resolved): the two silent warden.sh launches from session 4 needed no further re-diagnosis — one foreground smoke run surfaced the unbound variable immediately. Reproduce-foreground remains the right first move for silent launcher deaths.
