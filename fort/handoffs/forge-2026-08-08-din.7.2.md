# Handoff: Forge 2026-08-08

## Plan executed

1. Read the fort law, Forge protocol, GDD, G1 implementation, and the closed kqop ruling.
2. Add a durable, sim-time-keyed host notification queue with stable-ID idempotency, retry, and a PostgreSQL persistence adapter.
3. Wire last-revision warnings to an explicit HQ paper-projection provider on plan-change calls only, filter expired warnings, and run the full fort verifier.

## Clarifying questions

1. None. The closed kqop ruling specifies the paper-projection input, now-filtering, and warning trigger timing; din.11 explicitly owns delivered-entry retention/compaction policy.

Model: gpt-5.6-terra

## State of work

- `longburn-din.7.2` remains `in_progress` pending harness-owned review processing. The G2 durable notification queue is implemented; no retention or compaction policy was invented ahead of din.11.

## Verified facts

- `src/host/notification-queue.ts` persists G1 `NotificationMoment`s by their stable IDs, releases only when the injected persistent-anchor mapping reaches `deliverAtMs`, records failed attempts for retry, and retains delivered rows. A crash between transport and acknowledgement safely repeats the same ID.
- `db/migrations/0005_notification_queue.sql` creates the durable PostgreSQL queue and its due-entry index. As with all migrations, deployment/CI applies it, never sim code.
- `src/host/notification-plan-changes.ts` derives and enqueues revision warnings only when its plan-change entry point is called. Its provider is explicitly restricted to HQ's paper projection (command echoes plus received outcomes), never the authoritative worldline.
- `src/sim/notification-derivation.ts` documents that same input contract on `LastRevisionInstantInput` and requires `nowMs` to omit expired last-revision deadlines.
- Focused notification tests: 3 files, 11 tests passed. `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-din.7.2 fort/scripts/verify.sh` exited 0: 31 files / 193 tests passed; 4 PostgreSQL-gated integration tests skipped by the Forge sandbox. `verify.pass` was emitted.

## Next actions

1. End this Forge session for harness-owned verification and review dispatch.
2. Din.11 decides delivery retention and compaction cadence before any pruning is added.

## Open risks / questions

- The durable wall-clock anchor is owned by `longburn-9j0`; this queue intentionally depends on its injected mapping and cannot itself make a restarted world clock correct.
- A concrete push/email transport and its stable-ID deduplication are deferred to the remaining notification work. The queue exposes the stable ID required for that boundary.

## Failed attempts

- The worktree began without `vitest`; `npm ci --offline --no-audit --ignore-scripts` restored the lockfile-pinned toolchain from the local cache without changing dependency files.
- The outbound transport lint fence initially rejected the generic callback name `send`. Renaming the already-causal-derived adapter boundary to `deliver` preserves the no-raw-outbound fence while keeping transport implementation out of G2.
