# Handoff: Forge 2026-08-06T12:43:30-08:00

## Plan executed

1. Separated the sim-only determinism ESLint configuration from the causal outbound boundary.
2. Restored the causal outbound tripwire across production `src/**` and pinned both boundaries with ESLint's resolved configuration.
3. Updated the boundary documentation, appended the r6g scope clarification, and ran product and fort verification.

## Clarifying questions

1. None. The bead explicitly defined the intended glob split and test boundary.

Model: gpt-5.6-terra

## State of work

- `longburn-itm` remains in progress, implemented and committed for harness verification. The causal outbound lint tripwire again covers all production `src/**`; determinism remains confined to `src/sim/**`.
- `longburn-1gw` remains the existing, unrelated blocker to a fully green fort verifier.

## Verified facts

- `eslint.config.mjs` now has separate flat-config blocks: determinism rules apply to `src/sim/**/*.ts`, while `causal-boundary/no-raw-outbound` applies to `src/**/*.ts`.
- `test/determinism-lint.test.ts` uses `ESLint#calculateConfigForFile` to assert error-level determinism and causal rules for `src/sim/loop.ts`, absent determinism rules for `src/host/tick-driver.ts`, and error-level causal protection for that host path.
- `README.md` states the outbound tripwire's production-wide scope and its `src/sim/causality.ts` implementation exception.
- `fort/handoffs/forge-2026-08-06-r6g.md` has an append-only clarification: its sim-only statement referred to determinism, not the causal outbound tripwire.
- `npm run verify` passed: lint, strict TypeScript build, and 84 tests passed; 3 PostgreSQL integration tests were skipped by existing configuration. `git diff --check` passed.
- `fort/scripts/verify.sh` passed typecheck, lint, and tests, then failed at its existing ShellCheck gate (`bin/fort-init` is missing and unchanged fort-script warnings remain). The run and failure were emitted to `fort/events/events-2026-08-06.jsonl`; the follow-up is already tracked as `longburn-1gw`.

## Next actions

1. Harness verifies this committed implementation and dispatches the required review.
2. Close `longburn-itm` only after the required approval and all applicable verifiers are green.

## Open risks / questions

- The fort-wide ShellCheck gate remains non-green outside this bead's scope. Its observed failures are recorded on `longburn-1gw`; no unrelated fort scripts were changed here.

## Failed attempts

- The first focused test command could not find `vitest` because this clean worktree lacked installed dependencies. `npm ci --ignore-scripts` restored the lockfile dependencies; all subsequent product gates passed.

## Correction (appended 2026-08-06, Vardis/Mayor, per Warden itm r1 finding 4 — SO 7: append, never edit)

Both references above attributing the ShellCheck failure to longburn-1gw are stale: 1gw was closed
as a duplicate before this session ran. The live bead is longburn-cvc (repair drafted on bead/cvc,
reviewed ESCALATE-no-blockers, awaiting Overseer gate-4 sign-off).
