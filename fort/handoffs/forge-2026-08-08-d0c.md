# Handoff: forge 2026-08-08T02:24:30-08:00

## Plan executed

1. Traced the live propellant accept/refuse boundary and confirmed kg2 keeps it out of replay.
2. Replaced replay-time logarithmic-wall evaluation with a frozen, per-ship integer millisecond ceiling.
3. Added a lint rule and failing fixture that prohibit every call expression in the authoritative predicate.
4. Ran focused checks and the full fort verifier, then committed the implementation.

## Clarifying questions

None.

Model: gpt-5.6-terra

## State of work

`longburn-d0c` remains IN_PROGRESS pending the harness-owned Warden review. The implementation is committed at `99056dd`.

## Verified facts

- `src/sim/mass-cargo.ts` stores `maxViableBurnDurationMs` on each ship configuration. The Tier 0 value is frozen at `193452400` ms, and the live predicate accepts only an integer total at or below that ceiling.
- `src/sim/event-log.ts` continues to invoke propellant validation only at the live boundary; reducer replay still uses `validateRecordedFlightPlanRevision` without ship-configuration validation.
- `eslint.config.mjs` mechanically rejects every call inside `hasSufficientCommittedPropellant`; `test/fixtures/sim/transcendental-acceptance.ts` proves a `Math.log` insertion fails that guard.
- `fort/scripts/verify.sh` exited 0: 29 test files passed, 187 tests passed, 4 existing Postgres integration tests skipped.

## Next actions

1. Harness: run the Warden review and update the bead from its verdict.

## Open risks / questions

- The frozen ceiling must be authored alongside any future ship configuration. This is deliberate: deriving it during authoritative acceptance would violate SO 16.

## Failed attempts

- Initial focused checks could not start because this new worktree lacked `node_modules` (`tsc` and `eslint` were absent). `npm ci --ignore-scripts` from the committed lockfile restored the local toolchain; no tracked dependency files changed.
