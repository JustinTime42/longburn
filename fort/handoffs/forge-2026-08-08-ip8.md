# Handoff: Forge 2026-08-08T18:31:00-08:00

## Plan executed

1. Read the governing fort records, Forge protocol, bead, GDD, review notes, and preceding causal-egress handoff.
2. Bind every WebSocket subscription to a non-empty authoritative observer ID in the public egress API.
3. Refuse mismatched envelope and subscription observer IDs before invoking the causality gate or raw socket callback.
4. Add focused regression coverage, update the public boundary documentation, and run the fort verifier.
5. Commit the implementation, then record this append-only handoff.

## Clarifying questions

1. None. The bead specified both the changed subscription contract and the required mismatch refusal behavior.

Model: gpt-5.6-terra

## State of work

- `longburn-ip8` remains in progress pending harness-owned verification and review. Implementation is committed in `baf33ce`.

## Verified facts

- `CausalStateEgress.subscribe(observerId, socket)` now requires a non-empty observer ID and captures it in the subscription closure (`src/host/causal-state-egress.ts`).
- A candidate whose `observerId` differs from that binding returns the typed `{ sent: false, reason: "observer-mismatch" }` result without reaching either `CausalEmissionGate` or `socket.writeText` (`src/host/causal-state-egress.ts`, `src/host/causal-state-egress.test.ts`).
- Matching candidates retain the existing gate behavior. `snapshot()` remains gate-only and unchanged.
- `npm test -- src/host/causal-state-egress.test.ts`, `npm run typecheck`, and `npm run lint` passed.
- `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-ip8 fort/scripts/verify.sh` exited 0: typecheck and lint passed; 160 tests passed; 3 configured PostgreSQL integration tests skipped; shellcheck passed.

## Next actions

1. End this Forge session for harness processing.

## Open risks / questions

- No open implementation questions. This structural binding prevents a caller from routing observer A's envelope through observer B's subscription; full composed-path closure remains owned by `longburn-din.5.4`.

## Failed attempts

- `bd update longburn-ip8 --claim` correctly refused because the bead was already atomically claimed by `orin`; no state changed.
