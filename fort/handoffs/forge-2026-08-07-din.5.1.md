# Handoff: Forge 2026-08-07T17:20:00-08:00

## Plan executed

1. Read the fort charter, operational record, Forge protocol, Beads context, approved emitted-state catalog, causality design, founding specifications, and prior Forge handoff.
2. Define the emitted-message envelope and the catalog's seven discriminants, retaining market events as reserved and the two deliberately non-emitted classes as unconstructable.
3. Add fail-closed runtime validation, stable per-observer idempotence keys, stored-position copying, and youngest-event aggregate selection.
4. Add unit coverage, run the TypeScript, lint, test, and fort verification gates, then make path-scoped commits.

## Clarifying questions

None. The approved catalog resolves the interface and scope.

Model: gpt-5.6-terra

## State of work

- `longburn-din.5.1` remains in progress pending the harness-owned verification and review flow. Implementation commit `0d68763` supplies the E1 message contract; it intentionally adds neither transport nor scheduler behavior.

## Verified facts

- `src/sim/emitted-message.ts` defines the discriminated catalog, class-specific T0 payloads, runtime provenance validation, server-computed staleness, and stable IDs formed from observer, stream, youngest event sequence, and class.
- The stored-position rule is mechanical: `buildEmittedMessage` copies the persisted event position, and `src/sim/emitted-message.test.ts` mutates the source after construction to prove no resolver-derived or aliased position leaks into the envelope.
- Aggregates select the greatest event time, breaking a tie with stream/log sequence; their selected event also determines the idempotence key.
- Command echoes and sim-clock messages are enforced as observer-local zero-staleness messages. Market events remain reserved for din.6; ephemerides and live ship position fail runtime validation because T0 does not emit them.
- `fort/scripts/verify.sh` exited 0: typecheck and lint passed; 143 tests passed, with 3 configured PostgreSQL integration skips.

## Next actions

1. Harness-owned verification and Warden dispatch.
2. E2/E3 may consume this envelope at their scheduler and transport boundaries without expanding its catalog scope.

## Open risks / questions

- The monotone per-observer emission cursor is intentionally not implemented here. It belongs with E2/E3 transport/scheduler ownership; this schema provides the stable `messageId` it needs.

## Failed attempts

- None.
