# Handoff: Forge 2026-08-07 round two, longburn-40j.1

## Plan executed

1. Turn the Warden's overlapping-burn trace into a failing public-API regression test before changing implementation.
2. Enforce non-overlapping pending burns in the shared reducer and reject revisions that overlap an active burn before durable append.
3. Preserve malformed refused plans as opaque historical payloads while validating refusal reasons before append and on replay; fold the approved equality, unreachable-guard, and indentation fixes.
4. Run the fort verifier, correct the earlier handoff record append-only, and commit the code plus this handoff.

## Clarifying questions

1. None. The Mayor's round-two scope resolved the intended validation boundary: accepted plans are validated, while refused payloads are preserved as evidence.

Model: gpt-5.6-terra

## State of work

- `longburn-40j.1` round-two code fix is committed and ready for the harness-owned verification and review flow.
- Code commit: `596062ca65cdd695da2bd0381b7c7c997463b922` — `longburn-40j.1: prevent flight-plan log corruption`.

## Verified facts

- The required first regression test failed before the fix: the loop accepted an overlapping `a@0` (10 ms) / `b@5` plan. It now rejects that plan before durable append and leaves the stream empty.
- `assertPlan` rejects overlapping pending nodes on the shared reducer path; `validateFlightPlanRevision` separately rejects any node scheduled before an active burn's exact end boundary.
- `#remainingToNextBurnBoundary` now throws if either active or pending boundary is behind virtual time, rather than clamping an impossible negative gap to due-now.
- Refusal payloads are opaque on replay, so a malformed refused plan records without changing the pending plan or poisoning replay. Refusal reasons are validated before append and by the reducer on replay.
- Burn-node equality is explicit rather than JSON serialization; the unreachable `ship === undefined` return and mis-indented reducer brace are removed.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge fort/scripts/verify.sh` exited 0: typecheck, lint, **108 passed and 3 PostgreSQL integration tests skipped** (111 total), and shellcheck all passed. `git diff --check` passed immediately before the code commit.

## Next actions

1. The harness owns the remaining verification and review flow for `longburn-40j.1`.
2. Close the bead only after that flow is green.

## Open risks / questions

- Derived-phase correctness remains intentionally out of scope for bead `036`; broader FlightPlan generators remain bead `5es`; verifier count provenance remains bead `gc6`.

## Unrequested changes

- None.

## Failed attempts

- The required pre-fix overlap test failed because the invalid plan was accepted; this failure was the evidence used to drive the repair.
