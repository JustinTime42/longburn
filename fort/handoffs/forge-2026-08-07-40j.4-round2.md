# Handoff: Forge 2026-08-07 longburn-40j.4 round 2

## Plan executed

1. Trace the Warden finding from the projected-state entry point through Pareto assembly and continuum feasibility.
2. Thread the caller-supplied projected heliocentric state into the continuum departure geometry and solar-radius calculation.
3. Map the virtual origin time to UT, reject cells departing before it, compose the shared heliocentric-state type, and correct test names.
4. Add regression coverage, run the complete fort verifier, and commit this append-only handoff.

## Clarifying questions

1. None. The Overseer's R2 ruling explicitly selected the departure-state override and world-epoch ordering guard.

Model: gpt-5.6-terra

## State of work

- `longburn-40j.4` has its R2 correction implemented and is ready for the harness-owned continuation.

## Verified facts

- `ProjectedShipState` now extends `HeliocentricState`; its supplied position and velocity become `departureStateOverride` in `assembleCell`, including the continuum solar radius derived from its position magnitude.
- `ProjectedStatePlannerRequest` now requires `worldEpochUtDaysSinceJ2000`. It converts `origin.atMs` with `simTimeToUtDays` and raises `RangeError` when any candidate departs before the selected origin.
- The projected-state request intentionally excludes a caller-provided departure override, so its physics origin is always its declared `origin`.
- Tests cover a non-Earth projected origin changing `quotedDutyCycle`, the before-origin refusal, and accurately named origin/cell-source behavior.
- Amendment C sizing is recorded in the API documentation: projected-state assembly follows the same synchronous full-sweep path as standard assembly, so a 64k-cell call remains the accepted seconds-scale operation (about 3.77 s assembly, plus 186–235 ms generation), not a keystroke-time query.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge fort/scripts/verify.sh` exited 0: typecheck, lint, 118 tests passed, 3 PostgreSQL integration tests skipped without a configured database URL, and shellcheck passed. `git diff --check` also passed.
- Emitted append-only `verify.run` and `verify.pass` events for this run.

## Next actions

1. Harness owns the remaining verification and dispatch flow.

## Open risks / questions

- The planner deliberately does not propagate a burn sequence. The projection owner must provide a calculated heliocentric state and origin-consistent candidate cells; quantized commitment nodes remain the only sim input.

## Failed attempts

- The focused test initially used a 20-second origin with a same-epoch departure. The new UT guard correctly refused that incoherent fixture, so the current-state fixture now uses sim time zero and the future-node fixture uses its matching next-day departure.
