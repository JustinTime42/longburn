# Handoff: Forge 2026-08-07 longburn-1ls

## Plan executed

1. Read the charter, operational record, Forge protocol, Beads workflow, founding and flight-plan specifications, current bead, and relevant prior Forge handoffs.
2. Trace the HQ, command transport, persisted-envelope, ephemerides, trajectory-planning, and loop boundaries before changing any authority-bearing position provenance.
3. Stop before implementation because the required authoritative ship-worldline interface is absent, rather than fabricate one from advisory or incomplete data.

## Clarifying questions

1. What is the authoritative owner and replay representation of `shipPositionAt(time)`? The current committed `BurnNode` schema contains only execution time, kind, and burn duration, with no direction or initial heliocentric state. `ProjectedShipState` is explicitly planner-only advisory data (`src/planner/pareto.ts`), so using it for causal record provenance would violate SO 16 and the documented planner/sim boundary.
2. Should this bead extend the durable command/ship schema with a quantized trajectory/worldline representation, or should it receive a separately specified authoritative projection service whose outputs are persisted as command-record facts? Either answer determines the shared interface and replay contract. Without it, only the HQ resolver can be changed; the required production ship resolver and arrival-position provenance cannot be implemented safely.
