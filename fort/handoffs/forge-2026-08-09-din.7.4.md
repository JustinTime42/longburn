# Handoff: Forge 2026-08-09T20:16:12-08:00

## Plan executed

1. Read the fort law, Forge protocol, approved notification surface, and G1–G3 contracts.
2. Implement per-tester channel, digest, quiet-hours, N4 lead-time, copy, and instrumentation surfaces.
3. Carry N4's lead-time component into the stable ID, wire delivery/open instrumentation hooks, and verify the full fort.
4. Commit the implementation and record this append-only handoff.

## Clarifying questions

1. None. I read “per-tester” as an observer-scoped host preference record, and “digest mode” as a disposition that holds only N6 for a daily-digest assembler rather than changing its causal floor.

Model: gpt-5.6-terra

## State of work

- `longburn-din.7.4` remains `in_progress`, assigned to Orin. Implementation is committed in `50a05ce`; harness-owned review and closure remain pending.

## Verified facts

- Commit `50a05ce` implements the approved N1–N6 defaults, observer-scoped preference persistence, N6-only digest eligibility, and quiet-hours deferral. Quiet hours are wall-clock product policy and cannot alter a sim-derived earliest-permissible instant.
- N4 defaults to 12 h and 1 h lead times; customized lists are validated. Stable IDs include both the execute time and `lead:<milliseconds>`, so two warnings about one deadline cannot collide.
- The transport respects the selected push, email, or in-app channel. It no longer silently falls back from push to email. Successful delivery records the required `notificationDelivered` receipt.
- Push-click handling forwards only the stable ID. `PostgresNotificationInstrumentation.recordOpened` obtains timing and class fields from its durable delivery receipt, then writes the corresponding `notificationOpened` record without trusting client-supplied telemetry dimensions.
- The transport-agnostic copy template surface implements all approved §2 drafts. New migrations are `0007_notification_instrumentation.sql` and `0008_notification_preferences.sql`; no migration was applied.
- Focused notification/client suite: 6 files, 32 tests passed. `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-din.7.4 fort/scripts/verify.sh` was run bare and exited 0: 36 files / 233 tests passed; 2 PostgreSQL-gated files / 5 tests skipped because the Forge sandbox has no `LONGBURN_TEST_DATABASE_URL`.

Unrequested behavior changes: none.

## Next actions

1. End this Forge session for harness-owned verification, review, and bead disposition.

## Open risks / questions

- Daily-digest aggregation is deliberately left at the product disposition boundary. The surface ensures only N6 may enter that path; the host composition that batches and sends a daily digest needs to consume it when the client shell is assembled.
- The new migration SQL was not live-PostgreSQL exercised in the Forge sandbox. The established Mayor pre-review DB gate should apply it to a fresh test database and run the PostgreSQL integration path.

## Failed attempts

- The first focused run failed where old tests encoded the pre-ratification behavior: one single N4 notification at the deadline and email fallback whenever push was absent. The tests now assert the approved lead-time and per-class channel contract; no production behavior was weakened.
