# Handoff: Forge 2026-08-08 round 1

## Plan executed

1. Read the fort charter, operational record, Forge seat protocol, founding specification, Beads context, the existing E4 suite, and the Warden's r1 findings.
2. Replace the fabricated `arrivalRecorded` envelope and unwired resolver mock with a real `AuthoritativeSimLoop` arrival using a terminal dock 3.3 light-seconds from HQ and a deliberately disagreeing `eventPositionAt` resolver.
3. Feed the persisted `arrivalRecorded` record into the existing scheduler-to-egress composition, rename the transport-boundary claim, and mutation-check the production terminal-position branch.
4. Run focused and full verification, emit the Forge event, and commit this append-only handoff.

## Clarifying questions

1. None. The Warden's primary remedy maps directly to existing authoritative-loop and scheduler interfaces.

Model: gpt-5.6-terra

## State of work

- `longburn-din.5.4` remains `in_progress` for harness-owned review processing.

## Verified facts

- The named regression now creates an `AuthoritativeSimLoop` arrival at a dock 3.3 light-seconds from HQ. Its live `eventPositionAt` resolver returns HQ, deliberately disagreeing with the transit terminal position.
- The test reads the persisted `arrivalRecorded` record, asserts its stored `eventPosition` is the quantized terminal position, and derives the `ScheduledEmission` only from that record's stored provenance.
- At `ceil(light-time)-1` the scheduler defers the real persisted arrival; at `ceil(light-time)` egress emits the stored terminal position and correct staleness. This would fail if `loop.ts` selected the event-position resolver branch for a departed ship.
- Mutation check: temporarily changing the production arrival terminal-position selection to always call `eventPositionAt` made the new test fail with stored HQ `{ x: 0, y: 0, z: 0 }` versus expected terminal `{ x: 989315111, y: 0, z: 0 }`. The production source was restored before verification.
- The boundary test name now says `egress transport boundary`, not `real WebSocket transport`.
- Date-skew note: this follow-up record uses the bead's established `2026-08-08` date even though the launcher environment reported 2026-08-07; the pre-existing handoff has the same convention discrepancy.

## Verification

- Focused `npm test -- --run src/host/causal-state-egress.e2e.test.ts`: 4 passed.
- `npm run typecheck`: passed.
- `git diff --check`: passed.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-din.5.4 fort/scripts/verify.sh`: passed, with 27 test files passed, 1 skipped (28); 166 tests passed, 3 skipped (169).

## Next actions

1. End this Forge session for harness processing.

## Open risks / questions

- The existing non-blocking Warden findings remain explicitly out of this round's scope (`l4cw` and `m7wr` as recorded on the bead).
