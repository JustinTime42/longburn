# Handoff: Forge 2026-08-08T23:18:00-08:00

## Plan executed

1. Version each derived last-revision notification ID by its pending burn's `executeAtMs`.
2. Preserve supersession by routing the complete current warning set through existing reconciliation.
3. Reject last-revision IDs from `NotificationQueue.enqueue`, prove both regressions, and run the full fort verifier.

## Clarifying questions

1. None. The bead specified the identity component, reconciliation behavior, and public-enqueue guard.

Model: gpt-5.6-terra

## State of work

- `longburn-5h49` remains `in_progress`, assigned to Orin. The scoped implementation and regression coverage are committed in `d3f4d74`; harness-owned verification, review, and closure remain pending.

## Verified facts

- `deriveLastRevisionWarnings` carries the source burn's `executeAtMs`, and `deriveLocalNotifications` now emits `notification:last-revision:<nodeId>:<executeAtMs>`.
- A delivered warning no longer collides with a later revision of that node: the new deadline has a new ID, while reconciliation removes only pending superseded rows.
- `NotificationQueue.enqueue` rejects the last-revision ID namespace before any store enqueue, preserving reconciliation as its sole queue path.
- Focused tests: 16 passed across notification derivation, plan-change, and queue suites.
- `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-5h49 fort/scripts/verify.sh` passed: 32 files / 208 tests passed; 2 integration files / 5 tests skipped without `LONGBURN_TEST_DATABASE_URL`. Typecheck, lint, determinism, and causal transport checks passed.

## Next actions

1. End this Forge session for harness-owned processing.

## Open risks / questions

- The live PostgreSQL queue integration test remains intentionally skipped without `LONGBURN_TEST_DATABASE_URL`; its fixture was updated to the versioned ID scheme.

## Failed attempts

- `npm ci` could not execute its local `esbuild` binary in this worktree (`EPERM`). The existing dependency installation at `/home/justin/dev/longburn/node_modules` ran the focused tests and full verifier successfully; the temporary worktree link was removed afterward.
