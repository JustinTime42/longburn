# Handoff: Forge 2026-08-08

## Plan executed

1. Read the charter, operational record, Forge protocol, Beads context, founding spec, emitted-state catalog, causality design, and the latest relevant Forge handoff.
2. Make command identity a required revision-outcome fact, migrate both direct loop writers, and retain runtime validation for decoded persisted data.
3. Make host projection fail closed per stored record, omit durable-log command echoes until H1, and align notification derivation with the strengthened type.
4. Add regressions, run focused checks and the full fort verifier, emit the verification event, then commit this append-only handoff.

## Clarifying questions

1. None. The bead and Warden note specified the preferred type-rooted repair and the catalog specifies H1 as the echo reconstruction path.

Model: gpt-5.6-terra

## State of work

- `longburn-m1p2` remains `in_progress` pending harness-owned review processing. This commit repairs the host and notification derivation failure surface before din.8 and kyr consume persisted streams.

## Verified facts

- `planRevisionApplied` and `planRevisionRefused` now require a non-empty `commandId`; direct loop methods deterministically stamp their persisted outcomes, and the reducer rejects an unsafe decoded empty identity (`src/sim/event-log.ts`, `src/sim/loop.ts`, `src/sim/event-log.test.ts`).
- `CausalStateHost.run` contains projection failure per stored record, records `invalid-envelope`, increments the causality alert, marks that record blocked, and continues delivery of other records (`src/host/causal-state-host.ts`, `src/host/causal-state-host.test.ts`).
- Durable `commandIssued` records no longer project to `commandEcho`; observer-local echoes remain H1 reconnect-snapshot work and cannot accumulate in `deferred` (`src/host/causal-state-host.ts`, `README.md`).
- Notification derivation consumes the required command identity directly; it can no longer throw for the former valid-but-unprojectable event shape (`src/sim/notification-derivation.ts`).
- `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-m1p2 fort/scripts/verify.sh` exited 0: typecheck, lint, and 29 test files passed (175 tests); four configured PostgreSQL integration tests skipped. `verify.pass` was emitted for the bead.

## Next actions

1. End this Forge session for harness processing.

## Open risks / questions

- H1 must implement snapshot reconstruction of observer-local command echoes from durable `commandIssued` history. This change intentionally does not create a stale replay delivery path.

## Failed attempts

- The worktree initially lacked its local TypeScript toolchain (`tsc: command not found`). `npm ci --ignore-scripts` restored the lockfile-pinned dependencies without changing the manifest or lockfile.
- The first full verifier exposed an inbound-race test that selected the first command-ID outcome. Direct local outcomes now have IDs, so the test now correlates the outcome to its persisted `commandIssued` record.
