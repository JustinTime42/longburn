# Handoff: Forge 2026-08-07 longburn-brp

## Plan executed

1. Serialize all `AuthoritativeSimLoop` writers through one loop-owned promise chain.
2. Pin the host-tick/ship-commit interleave and retain terminal behavior for a genuine external store conflict.
3. Run the fort verifier, record the outcome, and commit the implementation with this handoff.

## Clarifying questions

1. None. The bead specified the local serialization boundary, required interleave, and external-conflict behavior.

Model: gpt-5.6-terra

## State of work

- `longburn-brp` remains in progress pending the harness-owned verification and review flow. Local loop writers are serialized; external writer conflicts still reach `HostTickDriver` as terminal errors.

## Verified facts

- `AuthoritativeSimLoop` queues `advance`, `commitShipOrder`, and `requestRandom` behind one private promise chain. The append expectation is therefore read only after all earlier local writers complete.
- `src/host/tick-driver.test.ts` holds a real host tick append in flight, issues a ship commitment, then verifies ordered events and consistent state without stopping the driver.
- The same test file uses two loop instances against `InMemorySimulationEventStore` to verify a second-process-style stale sequence conflict still rejects the driver tick and stops the driver.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge fort/scripts/verify.sh` passed with exit code 0: typecheck, lint, 106 tests passed, 3 configured PostgreSQL integration tests skipped without `LONGBURN_TEST_DATABASE_URL`, and shellcheck passed.

## Next actions

1. Harness owns verification recording and Warden dispatch for `longburn-brp`.
2. Close the bead only after the harness review is green.

## Open risks / questions

- None discovered within this bead's scope.

## Failed attempts

- The worktree initially had no dependencies. `npm ci` failed because the sandbox forbids executing its worktree-local `esbuild` binary. The incomplete install was moved recoverably to `/tmp/brp-node_modules-npm-ci-failed`; the worktree now uses the primary checkout's lockfile-pinned `node_modules` via an ignored symlink, allowing the normal fort verifier to pass.
