# Handoff: Forge 2026-08-07T09:50:30-08:00

## Plan executed

1. Extend the authoritative committed-order schema with an integer simulation-clock departure epoch and expose it at the H2 command boundary.
2. Keep committed ships docked until that epoch, append the docked-to-accel transition at its exact simulation time, and anchor scheduled decisions to departure.
3. Cover command refusals, live phase splitting, and persisted replay; run the fort verifier and commit the implementation.

## Clarifying questions

1. None. The dispatch fixed the persisted representation, boundary validation, phase behavior, and decision-window anchoring.

Model: gpt-5.6-terra

## State of work

- `longburn-a0p` remains in progress pending the harness-owned verification and review flow. Implementation is committed in `32364971cd9a5aaedd1a730782fdb6e6b77e0f86`.

## Verified facts

- `CommittedShipOrder.departureAtMs` is a persisted `SimTimeMs`; its reducer validation rejects non-integer or negative epochs.
- `CommitShipOrderRequest.departureAtMs` is the H2-facing landing point for a selected `ParetoPoint.departureUtDays` after H2 maps UT onto the simulation clock. The command boundary rejects invalid epochs and `departure-before-commit` before calling the planner or appending an event.
- The reducer creates committed ships in `docked`; `AuthoritativeSimLoop` splits the tick at `departureAtMs` and appends `shipPhaseChanged: accelBurn` at that exact time. Immediate departure remains an exact-time transition.
- Re-target opens at departure and closes at the earlier of departure plus six hours or arrival. Arrival-profile stays flip-to-arrival.
- `src/sim/event-log.test.ts` directly replays a docked-to-accel branch; `src/host/ship-orders.test.ts` covers the live loop's docked wait and exact boundary transitions, then resumes a persisted journey.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge fort/scripts/verify.sh` exited 0: typecheck, lint, 107 tests passed, 3 configured PostgreSQL integration tests skipped without `LONGBURN_TEST_DATABASE_URL`, and ShellCheck passed. `git diff --check` also passed.

## Next actions

1. Harness owns the remaining verification recording and review dispatch for `longburn-a0p`.

## Open risks / questions

- None discovered within this bead's scope.

## Failed attempts

- The worktree initially lacked dependencies, so `tsc` was unavailable. A temporary symlink to the primary checkout's lockfile-pinned `node_modules` enabled local verification; it was moved recoverably to `/tmp/longburn-a0p-node_modules-symlink` before committing, leaving the worktree clean.
