# Handoff: Forge 2026-08-08T18:32:00-08:00

## Plan executed

1. Read the fort charter, operational record, Forge protocol, Beads context, governing Tier 0 specification, review notes, emitted-state catalog, and the relevant E2 handoff.
2. Trace the scheduler cursor path and its existing tests to establish what can and cannot detect a missing global position.
3. Document and enforce ordered scheduler input, expose below-cursor suppression through a dedicated delivery-integrity counter, and document the per-observer single-writer requirement.
4. Add focused regression coverage, update the operational README, run the full fort verifier, and commit this append-only handoff with the implementation.

## Clarifying questions

1. None. The bead precisely scopes observability and documentation. Global positions may legitimately have gaps, so detecting a completely omitted event is impossible at this boundary; counting its later below-cursor presentation is the observable precondition violation required by the bead.

Model: gpt-5.6-terra

## State of work

- `longburn-36t` remains `in_progress` for harness verification and Warden dispatch. The implementation is committed but the Forge does not launch review or close beads.

## Verified facts

- `EmissionScheduler.run()` now requires strictly ascending source global positions and rejects an unordered pass before emitting, while its public contract states that every pass must include all unacknowledged light-lagged emissions for that observer.
- `EmissionSchedulerOptions` now requires `incrementBelowCursorSuppression`; each light-lagged message at or below the saved cursor increments it rather than disappearing through a bare `continue`. This is deliberately separate from the causality-failure counter.
- The public scheduler and README state the single-writer assumption: callers serialize `run()` per observer, because a compare-and-advance cursor detects a losing advance but cannot prevent concurrent duplicate transport sends.
- `src/sim/emission-scheduler.test.ts` covers the observable late omitted-report suppression and rejects unordered inputs before any emitter call.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-36t fort/scripts/verify.sh` exited 0: typecheck, lint, test, and shellcheck passed; 160 tests passed and 3 configured PostgreSQL integration tests were skipped in the Forge sandbox.

## Next actions

1. End this session for harness verification and Warden dispatch.

## Open risks / questions

- The scheduler cannot discover a light-lagged event omitted forever, because valid global positions can contain unrelated-stream gaps. The caller contract and observable counter cover the point at which such an event is later presented below the cursor.
- H1's reconnect snapshot reconstruction remains unimplemented. The README now describes it as a planned required path rather than an existing behavior.

## Failed attempts

- None.
