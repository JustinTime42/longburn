# Handoff: Forge 2026-08-07T12:16:00-08:00

## Plan executed

1. Inspect the approved flight-plan specification, existing causal solver, event reducer, and serialized simulation loop.
2. Add a T0 Earth-HQ PlanRevision transport that solves inbound light time with fixed iterations and queues exact simulation-time arrival.
3. Apply or typed-refuse queued revisions inside the loop, with burn-first equal-time race ordering; add arrival, acceptance, interleaved-rejection, and malformed-payload tests.
4. Run product and fort verification, then commit the implementation and this append-only handoff.

## Clarifying questions

1. None. The bead, approved flight-plan specification, and dispatch addendum defined the command boundary and race rule.

Title: Command transport: PlanRevision at c from HQ — light-time solve, arrival scheduling, arrival-time validity, race rule, typed refusal events
Model: gpt-5.6-terra

## State of work

- `longburn-40j.2` remains in progress pending harness-owned verification and review. Implementation commit: `ae250b3`.

## Verified facts

- `src/host/plan-revision-transport.ts` issues from the imported fixed Earth HQ constant and uses the shared causality solve for the propagated ship worldline.
- `src/sim/loop.ts` schedules arrivals at exact simulation boundaries, validates only inside writer serialization, and records a typed refusal when an issued replacement races a started burn.
- `src/sim/causality.ts` runs 32 light-cone iterations for every solve; it evaluates convergence only after the fixed count.
- `src/host/plan-revision-transport.test.ts` contains 4 command-transport tests, including the blocked-tick interleaved rejection case.
- `npm run verify` passed: 112 tests passed; 3 PostgreSQL integration tests skipped because `LONGBURN_TEST_DATABASE_URL` is not configured.
- Bare `CI=1 fort/scripts/verify.sh --no-emit` passed with exit code 0: 112 tests passed; 3 PostgreSQL integration tests skipped.

## Next actions

1. Harness owns verification recording and Warden dispatch for `longburn-40j.2`.

## Open risks / questions

- None observed.

## Failed attempts

- The worktree initially lacked local development dependencies; `npm ci --ignore-scripts` restored lockfile-pinned dependencies without tracked manifest changes.
- The first fixed-iteration solver margin was one floating-point unit below the independent moving-receiver oracle in property tests; preserving the final nonzero iteration margin plus a scale-aware rounding margin resolved it.

## Unrequested changes

- None.
