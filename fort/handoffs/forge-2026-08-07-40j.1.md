# Handoff: Forge 2026-08-07 longburn-40j.1

## Plan executed

1. Retire the single-order state machine, its scheduled decision windows, and the obsolete REST command surface.
2. Replace it with quantized `BurnNode` and editable `FlightPlan` aggregates, append-only burn and plan-revision events, and a shared reducer invariant on direct and durable replay.
3. Derive ship phases from burn history and pending nodes, add replay-equivalence and immutability coverage, run the fort verifier, emit its result, and commit this handoff.

## Clarifying questions

1. None. The approved flight-plan model specified the replacement boundary and allowed the exact quantized node layout as an implementation decision.

Model: gpt-5.6-terra

## State of work

- `longburn-40j.1` is implemented and awaits the harness-owned verification and review flow.

## Verified facts

- `BurnNode` carries a branded simulation timestamp, validated kind, and `QuantizedBurnParameters`; `FlightPlan` permits only strictly ordered, uniquely identified pending nodes.
- `planRevisionApplied`, `planRevisionRefused`, `burnStarted`, and `burnEnded` are reduced by the shared `SimEventReducer`. No event type can delete or edit an executed burn.
- A live revision is validated before durable append, and both direct plus persisted replay reject an attempted reintroduction of an executed node.
- `AuthoritativeSimLoop` appends burn boundaries at exact virtual-clock times and keeps the local writer serialization from longburn-brp. `docked`, `accel`, `coast`, `flip`, `decel`, and `arrived` are derived views, never event payloads or mutable state.
- The replay property covers an executed node plus a revised pending node and compares a resumed persistent stream to the live state.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge fort/scripts/verify.sh` exited 0: typecheck, lint, 107 tests passed, 3 PostgreSQL integration tests skipped without a configured database URL, and shellcheck passed. `git diff --check` passed.
- Emitted append-only `verify.pass` for `longburn-40j.1`.

## Next actions

1. Harness owns verification recording and Warden dispatch for `longburn-40j.1`.
2. Close the bead only after the harness review is green.

## Open risks / questions

- `planRevisionRefused` is intentionally recorded by the loop but its light-time arrival validation and refusal-reason selection belong to blocked bead `longburn-40j.2`.
- Fuel validation and planner projection remain deliberately outside this core schema bead.

## Failed attempts

- The worktree had no local `node_modules`; a temporary symlink to the main checkout's lockfile-pinned dependencies enabled verification and was moved recoverably to `/tmp/longburn-40j.1-node_modules-symlink` afterward.
