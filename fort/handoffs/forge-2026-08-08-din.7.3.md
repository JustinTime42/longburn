# Handoff: Forge 2026-08-08T23:20:00-08:00

## Plan executed

1. Inspected G1/G2's `NotificationMoment` and delivery boundary plus the
   approved notification product-surface specification.
2. Implemented a timing-free transport adapter with web push as primary,
   email only when no browser subscription is registered, and stable queue IDs
   passed as provider idempotency keys.
3. Added durable PostgreSQL subscription storage, browser service-worker
   registration and worker-side notification rendering, plus focused tests.
4. Ran focused tests and the full fort verifier, then recorded the handoff for
   harness-owned processing.

## Clarifying questions

1. None. The bead and approved product surface supply the required channel
   policy; copy is correctly left injectable for G4's approved template surface.

Model: gpt-5.6-terra

## State of work

- `longburn-din.7.3` remains `in_progress`, assigned to Orin. Implementation
  is complete in this session and awaits harness-owned verification/review.

## Verified facts

- `NotificationTransport` accepts only G1-derived `NotificationMoment`s via
  G2's existing `deliver` boundary; it contains no sim-time or retry logic.
- Registered web-push subscriptions receive the same rendered payload and
  stable notification ID as idempotency key. Email is selected only when the
  tester has no registered push subscription.
- Migration `0006_notification_push_subscriptions.sql` persists browser
  endpoint and encryption keys, while VAPID private material and SMTP
  credentials are injected host configuration and never stored or read here.
- The client registration helper reuses an existing subscription or subscribes
  with the VAPID public key; the worker displays only transport-provided content.
- Focused transport/client tests passed: 3 files, 8 tests.
- `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-din.7.3 fort/scripts/verify.sh`
  passed with typecheck, lint, determinism, causal transport fence, and
  shellcheck green: 35 files / 214 tests passed; 2 integration files / 5 tests
  skipped because their live PostgreSQL environment gate was absent.

## Next actions

1. End this Forge session for harness-owned verification and dispatch.

## Open risks / questions

- The concrete provider clients (VAPID HTTP signing and SMTP credentials) are
  deliberately injected adapter responsibilities. This work stores only public
  browser capabilities and does not touch secret sources or launchers.
- Browser subscription upload endpoint composition awaits the client-shell host;
  the registration helper exposes an explicit `storeSubscription` boundary.

## Failed attempts

- The initial `npm ci` postinstall could not spawn esbuild in this sandbox
  (`EPERM`). `npm ci --ignore-scripts` supplied the lockfile-pinned tooling;
  direct typecheck, lint, focused tests, and the full fort verifier then passed.
- The first event command used positional arguments incorrectly. A corrective,
  append-only event records that malformed metadata; no existing record was
  altered.
