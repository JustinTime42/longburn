# Handoff: Forge 2026-08-04T14:03:43-08:00

Model: GPT-5.6 Codex

## State of work

- `longburn-din.1` remains in progress and is ready for a fresh Warden review. Commit `85e8ab1` remediates the disposition recorded on the bead after Warden review 1.

## Verified facts

- The 2%-of-SOI acceptance criterion is now mechanically enforced in `src/sim/ephemerides.test.ts`: Earth and Mars heliocentric limits are bounded by their SOIs, and the planning-relevant lunar geocentric limit is 15 km, below 2% of the Moon's SOI. The 1,652 km heliocentric lunar error deliberately is not compared to the Moon's SOI because it includes Earth's heliocentric error.
- The test dynamically resets modules and imports the adapter cold after poisoning delta-T; it proves initialization restores JPL Horizons delta-T before any lookup, then proves byte-identical output after warmed and poisoned call histories. The dynamic process-global state is restored in `finally`, and the normal module copy is restored in `afterEach`.
- Fixtures now assert 97 samples per Earth/Moon/Mars payload. The geocentric lunar maxima are pinned at 13.946 km and 5.19e-5 km/s. Kilometer-scale pins use precision 3 for libm portability.
- `docs/decisions/ephemerides.md` contains an additive validation result: the 2026-01-01 through 2027-04-26 window, observed errors, accepted thresholds, and the full-range 4,405 km Mars result superseding the spike's 1,982 km sample.
- `npm run verify` passed at `85e8ab1`: ESLint, `tsc --noEmit`, and Vitest (12 tests across 5 files). `git diff --check` passed.

## Next actions

1. Warden performs a fresh review of `85e8ab1` against the round-2 routing on `longburn-din.1`.
2. If approved, the harness may close the bead. Forge must not merge or push.

## Open risks / questions

- `fort/reviews/longburn-din.1-review-1.md` was referenced in the bead but was not present in this worktree. The bead's full comment supplied the required disposition; no review record was altered or recreated.
- The validated-window boundary is documented only. Refusal behavior remains correctly deferred to `longburn-8fo`.

## Failed attempts

- Applying the SOI assertion directly to the 1,700 km heliocentric lunar threshold failed because 2% of the stated 66,000 km lunar SOI is 1,320 km. The measured 13.946 km geocentric lunar error is the correct SOI-relative quantity, so the enforcement was placed on a 15 km geocentric limit instead.
