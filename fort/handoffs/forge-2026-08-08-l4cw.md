# Handoff: Forge 2026-08-08, longburn-l4cw

## Plan executed

1. Read the charter, operating record, Forge seat protocol, founding specifications, Beads context, and the existing E4 composition suite.
2. Strengthen the scheduler-to-egress E4 suite with tick-stepped delivery, a moving-observer retarded-time oracle, composition-bound boundary cases, per-event arrival ordering, and decoded-wire validation.
3. Run focused and fort-wide verification, emit the verification record, and commit this append-only handoff.

## Clarifying questions

1. None. The bead maps directly to the existing `CausalStateHost` composition and E4 test fixture.

Model: gpt-5.6-terra

## State of work

- `longburn-l4cw` implementation is complete and remains `in_progress` for harness-owned processing.

## Changes

- The 40-day seeded stream now runs at every report's independent `earliest - 1` and `earliest` ticks, proving defer-then-deliver behavior through the delivery cursor across virtual-clock passes.
- Added an independent constant-velocity retarded-time oracle for a moving Earth observer, with roughly 0.07 light-seconds of movement across a Mars light time.
- Routed the integral light-cone boundary through `CausalStateHost` instead of directly invoking egress.
- Added the requested E2E per-event ordering dispute case: a near report delivers while a farther report remains deferred in the same real `CausalStateEgress` composition pass.
- The socket fixture now parses JSON and calls `validateEmittedMessage`, so the test re-validates the actual wire payload.

## Verification

- Focused `npm test -- --run src/host/causal-state-egress.e2e.test.ts`: 5 passed.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-l4cw fort/scripts/verify.sh`: passed.
  - 29 test files passed, 1 skipped (30).
  - 176 tests passed, 4 skipped (180).
- `git diff --check`: passed.

## Surprises

- This worktree has no installed `node_modules`. For verification only, I temporarily linked the main worktree's existing dependencies, ran the verifier, then restored the original local cache directory. No dependency files are part of the change.
