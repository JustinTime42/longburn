# Handoff: Forge 2026-08-08T18:18:00-08:00

## Plan executed

1. Read the charter, operational record, Forge protocol, Beads context, governing causality design, emitted-state catalog, relevant specifications, and the prior stopped E2 handoff.
2. Apply the recorded addendum: durable delivery applies only to light-lagged classes; E2 owns the cursor interface, in-memory reference store, Postgres adapter, and migration.
3. Implement an explicit-sim-time scheduler that derives earliest delivery from the stored event position, fails closed on malformed scheduling provenance, and advances a cursor only after acknowledgement.
4. Add cursor-contract and scheduler restart/causality tests, update the operational README, run the full fort verifier, then commit the implementation and this append-only handoff.

## Clarifying questions

1. None. The catalog addendum answered both questions from the prior handoff.

Model: gpt-5.6-terra

## State of work

- `longburn-din.5.2` remains `in_progress` for harness verification and Warden dispatch. This handoff records the implemented E2 boundary; it does not launch review or close the bead.

## Verified facts

- `EmissionScheduler` receives its clock tick as `SimTimeMs`, builds candidates only from the event's stored provenance, uses the shared fixed-point solve's ceiling tick, and refuses to call its emitter before that tick.
- Its pre-gate computation path fails closed for malformed provenance, records an `invalid-position` incident, increments the causality counter, and never calls the emitter. The gate remains the final assertion on every actual emission.
- `DeliveryCursorStore` has in-memory and Postgres implementations. The shared cursor contract establishes empty/read/copy/strict-monotone behavior; the Postgres leg uses a compare-and-advance UPSERT and migration `0003_delivery_cursors.sql`.
- Cursor advancement happens only after the injected E3-facing emitter returns `{ sent: true }`. A failed transport leaves no cursor, so a restart redelivers stable message IDs; once acknowledged, the persisted watermark suppresses re-leaks without skipping the next scheduled report.
- Observer-local `commandEcho` and `simClock` messages do not advance or replay through the durable cursor. The scheduler permits them only on their exact event tick; H1 reconnect reconstructs missed local projections from durable command history as directed by the catalog addendum.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge FORT_TARGET=longburn-din.5.2 fort/scripts/verify.sh` exited 0: typecheck, lint, test, and shellcheck passed; 153 tests passed and 3 configured PostgreSQL integration tests were skipped in the Forge sandbox.

## Next actions

1. End this session for harness verification and review dispatch.

## Open risks / questions

- The Postgres integration suite still needs a provisioned `LONGBURN_TEST_DATABASE_URL`; this is the existing Forge-sandbox skip, not a bypass of the in-memory/Postgres cursor contract tests.

## Failed attempts

- None.
