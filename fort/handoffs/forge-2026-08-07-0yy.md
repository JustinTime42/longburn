# Handoff: Forge 2026-08-07T16:07:00-08:00

## Plan executed

1. Located the absolute Kepler alpha and element thresholds and mapped their units to the current radius and angular-momentum magnitude.
2. Replaced absolute alpha decisions with `1e-12 / r` and node singularity decisions with `1e-10 * |h|`; retained the eccentricity threshold as dimensionless.
3. Added heliocentric near-parabolic and near-equatorial regressions, then ran the complete fort verifier.

## Clarifying questions

None. The bead specified the reference scales (`r0` and `|h|`) unambiguously.

Model: gpt-5.6-terra

## State of work

- `longburn-0yy` remains in progress pending the harness-owned review flow. Its implementation and verifier work are complete in this worktree.

## Verified facts

- `src/sim/kepler.ts` now scales the universal-variable parabolic branch by the initial radius and scales the element node singularity by angular-momentum magnitude. Eccentricity continues to use the same dimensionless tolerance.
- `src/sim/kepler.test.ts` contains regressions that require a near-parabolic heliocentric orbit to retain a finite semi-major axis and a `sin(i) = 1e-11` heliocentric orbit to use equatorial conventions.
- `fort/scripts/verify.sh` exited 0: typecheck, lint, and 132 tests passed; 3 existing PostgreSQL integration tests were skipped by configuration.

## Next actions

1. Let the harness perform its standard verification and review dispatch.
2. Close `longburn-0yy` only after that review is recorded as green.

## Open risks / questions

- None discovered.

## Failed attempts

- The initial regression used Vitest's unsupported `toBeFinite` matcher. Replaced it with `Number.isFinite`; no implementation failure occurred.
