# Handoff: Forge 2026-08-04T11:23:00-08:00

Model: GPT-5.6 Codex

## State of work

- `longburn-din.1` remains in progress, pending Warden review. The required Horizons fixture validation gate is implemented and green in commit `c8cf635`.

## Verified facts

- The checked-in raw JPL Horizons VECTORS payloads cover 97 TDB epochs for each Tier 0 body. `src/sim/ephemerides.test.ts` parses every vector, maps TDB to the adapter's UT J2000-day input with `AstroTime.FromTerrestrialTime` and Astronomy Engine's pinned JPL Horizons delta-T convention, then compares the same heliocentric EQJ/km/km-s state.
- Maximum observed position errors are Sun 0 km, Earth 1645.534789 km, Moon 1652.147992 km, and Mars 4404.986594 km. Corresponding maximum velocity errors are 0, 0.001501137, 0.001514090, and 0.000938804 km/s. The contract limits are deliberately narrow and data-derived: 0/1700/1700/4500 km and 0/0.0016/0.0016/0.001 km/s.
- The fixture-derived lunar perigee is JD TDB 2461206.5 (357197.239948 km), apogee is 2461386.5 (406215.945938 km), and the sampled 2026 solar-conjunction window is 2461051.5 (178.275711835 degrees heliocentric Earth-Mars separation).
- `src/sim/ephemerides.ts` pins `SetDeltaTFunction(DeltaT_JplHorizons)` at module initialization and before each conversion/state query. Cold, warmed, interleaved, and externally poisoned delta-T call histories remain byte-identical.
- `npm run verify` passed: ESLint, strict TypeScript build, and Vitest (12 tests across 5 files). `git diff --check` passed before commit.

## Next actions

1. Warden reviews commit `c8cf635`, including its raw-fixture provenance and the data-derived Mars threshold.
2. On Warden approval, close `longburn-din.1`; this unblocks trajectory-planner bead `longburn-din.3`.

## Open risks / questions

- Mars's 4405 km maximum exceeds the spike's earlier illustrative ~1982 km figure, but is still about 0.8% of the stated 577000 km Mars SOI. The threshold is 4500 km, only 95 km above the fixture result; Warden should explicitly accept this range-specific finding.
- The TDB-to-TT approximation is documented in code. Its periodic difference is milliseconds, materially below the provider deltas; the pinned delta-T conversion then establishes the UT adapter input.

## Failed attempts

- `@types/node` is not a project dependency, so the fixture parser uses TypeScript JSON imports with `resolveJsonModule` rather than Node filesystem APIs. The resulting strict build and test suite are green.
