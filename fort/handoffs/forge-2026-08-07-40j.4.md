# Handoff: Forge 2026-08-07 longburn-40j.4

## Plan executed

1. Trace the flight-plan state boundary and existing Pareto planner API.
2. Add a planning-layer entry point that accepts a caller-propagated ship state at any chosen virtual-clock instant and derives origin-specific candidate cells from it.
3. Preserve the projected origin with the advisory result, prove current and post-node planning in tests, run the fort verifier, emit its result, and commit this handoff.

## Clarifying questions

1. None. The approved flight-plan model clearly assigns propagation to the client/planner boundary and preserves quantized nodes as the only commitment payload.

Model: gpt-5.6-terra

## State of work

- `longburn-40j.4` is implemented and awaits the harness-owned verification and review flow.

## Verified facts

- `ProjectedShipState` carries an explicit virtual-clock epoch plus heliocentric position and velocity, outside authoritative simulation state.
- `ProjectedStateCellSource` receives that exact selected origin, so current-state and post-node planning can use origin-specific candidate-cell searches without inventing a directional vector from quantized burn durations.
- `tier0TrajectoryPlanner.planFromProjectedState` is synchronous and local, takes no transport or light-time argument, and preserves the advisory origin alongside its Pareto landscape. Only a later `PlanRevision` crosses the causal command boundary.
- Existing `assemble` consumers remain compatible; `planFromProjectedState` is the H2-facing extension.
- `src/planner/pareto.test.ts` covers both a current propagated state and a future post-outbound-node state.
- Bare `FORT_ACTOR=orin FORT_SEAT=forge fort/scripts/verify.sh` exited 0: typecheck, lint, 116 tests passed, 3 PostgreSQL integration tests skipped without a configured database URL, and shellcheck passed. `git diff --check` passed.
- Emitted append-only `verify.pass` for `longburn-40j.4`.

## Next actions

1. Harness owns verification recording and Warden dispatch.
2. Close the bead only after the harness review is green.

## Open risks / questions

- Physical propagation from the quantized node sequence is intentionally not introduced here: burn duration contains no direction, and fabricating it would violate the planner/advisory boundary. A projection owner must provide the calculated heliocentric state to this API.

## Failed attempts

- This worktree had no local development dependencies, so the first typecheck reported `tsc: command not found`. `npm ci --ignore-scripts` restored lockfile-pinned dependencies; no tracked dependency file changed.
